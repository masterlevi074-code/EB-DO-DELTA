--========================================================--
--                     NOXYS HUB                         --
--              TACTICAL MOBILE EDITION                  --
--========================================================--

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local TeleportService = game:GetService("TeleportService")

local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")
local Workspace = game:GetService("Workspace")

--========================================================--
-- AUTO-LIMPEZA
--========================================================--

if CoreGui:FindFirstChild("WindUI") then CoreGui.WindUI:Destroy() end
if PlayerGui:FindFirstChild("WindUI") then PlayerGui.WindUI:Destroy() end
if CoreGui:FindFirstChild("DashButtonScreen") then CoreGui.DashButtonScreen:Destroy() end
if CoreGui:FindFirstChild("NoxysTacticalHub") then CoreGui.NoxysTacticalHub:Destroy() end
if PlayerGui:FindFirstChild("NoxysTacticalHub") then PlayerGui.NoxysTacticalHub:Destroy() end

--========================================================--
-- VARIÁVEIS GLOBAIS
--========================================================--

local dashStuds = 30
local dashButtonScreen = nil

local ESP_GlobalAtivado = false
local ESP_Chams = false
local ESP_Box = false
local ESP_Names = false
local ESP_Tracers = false
local ESP_Skeleton = false
local ESP_Vida = false
local ESP_TimesNomes = false
local ESP_Distancia = false
local ESP_CoresPorTime = false
local ESP_BoxThickness = 2
local CorESP = Color3.fromRGB(255, 50, 50)
local LinhasTracers = {}
local CaixasESP2D = {}
local ESP_BoxMaxDistance = 1500

local ESP_TimesAtivado = false
local TimesMarcados = {
    ["[CI]"] = false, ["[EB]"] = false, ["[OF]"] = false,
    ["[BAC]"] = false, ["[BPE]"] = false, ["[BFE]"] = false,
    ["[CIE]"] = false, ["[BIP]"] = false, ["[CAAT]"] = false,
    ["[CIGS]"] = false, ["[MD]"] = false, ["[REC MEC]"] = false,
}

local Aimbot_Ativado = false
local Aimbot_UsarFOV = false
local Aimbot_RaioFOV = 100
local Aimbot_EspessuraFOV = 1.5
local Aimbot_CorFOV = Color3.fromRGB(255, 255, 255)
local Aimbot_Suavidade = 0.15
local Aimbot_ParteAlvo = "Head"
local Aimbot_TeamCheck = false
local Aimbot_OffsetTelaY = -40
local Aimbot_OffsetPixels = 40
local Aimbot_WallCheck = false
local Aimbot_PrioridadeDistancia = false
local Aimbot_DestacarAlvo = false
local AimbotAlvoAtual = nil
local AimbotAlvoHighlight = nil
local Aimbot_SoComItem = false

local Hitbox_Ativado = false
local Hitbox_Tamanho = 20
local Hitbox_Transparencia = 0.7
local Hitbox_Cor = Color3.fromRGB(255, 0, 0)
local Hitbox_Material = Enum.Material.Neon

local AutoJJ_Ativado = false
local AutoJJ_Script = nil

local BlocosAtivado = false
local Hitboxes = {}

local SpeedHack_Ativado = false
local SpeedHack_Velocidade = 50
local SpeedHack_Conexao = nil

local Underground_Ativado = false
local Underground_Conexao = nil
local Underground_yAbaixo = -90
local Underground_yCamera = 4.7
local Underground_Voltando = false
local Underground_CameraPart = nil
local UndergroundFloor = nil

local PasseLivre_Ativado = false
local PasseLivre_Conexao = nil
local PasseLivre_Centro = Vector3.new(-13, 3.0, -494.5)
local PasseLivre_Raio = 3
local PasseLivre_NoClipAtivo = false

local Admin_Ativado = false
local Admin_Prefixo = "!"
local Admin_Invisivel = false

local ChatWindow_Ativado = false
local ChatWindow_Conexoes = nil

local JogadoresIgnorados = {}
local DropdownSelecionado = ""

local FOVCircle = Drawing.new("Circle")
FOVCircle.Visible = false
FOVCircle.Color = Aimbot_CorFOV
FOVCircle.Thickness = Aimbot_EspessuraFOV
FOVCircle.Radius = Aimbot_RaioFOV
FOVCircle.Filled = false

--========================================================--
-- TAMANHO
--========================================================--

local UI_WIDTH = 280
local UI_HEIGHT = 340

local MIN_WIDTH = 240
local MAX_WIDTH = 420

local MIN_HEIGHT = 260
local MAX_HEIGHT = 500

--========================================================--
-- CORES TÁTICAS
--========================================================--

local COLORS = {
	Background = Color3.fromRGB(12, 15, 13),
	Panel = Color3.fromRGB(20, 24, 21),
	Panel2 = Color3.fromRGB(27, 32, 28),
	Olive = Color3.fromRGB(76, 96, 60),
	OliveDark = Color3.fromRGB(49, 62, 41),
	Green = Color3.fromRGB(78, 195, 98),
	GreenDark = Color3.fromRGB(46, 105, 57),
	Yellow = Color3.fromRGB(190, 171, 77),
	Text = Color3.fromRGB(232, 235, 229),
	Muted = Color3.fromRGB(143, 151, 141),
	Stroke = Color3.fromRGB(56, 67, 55)
}

local Accent = COLORS.Olive

--========================================================--
-- CONFIGURAÇÃO
--========================================================--

local Config = {
	FOV = 70,
	FOVEnabled = true,
	Notifications = true
}

--========================================================--
-- SCREEN GUI (COREGUI PARA SOBREPOR TUDO)
--========================================================--

local Gui = Instance.new("ScreenGui")
Gui.Name = "NoxysTacticalHub"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Gui.DisplayOrder = 999
Gui.Parent = CoreGui

local FunctionDescriptions = {
    ["ATIVAR ESP"] = "Liga ou desliga todos os recursos visuais do ESP.",
    ["ESP BOX"] = "Mostra uma caixa 2D ao redor dos jogadores.",
    ["ESP NAMES"] = "Mostra o nome dos jogadores.",
    ["ESP TIMES"] = "Mostra o nome do time acima do jogador.",
    ["ESP DISTÂNCIA"] = "Mostra a distância até cada jogador.",
    ["ESP VIDA"] = "Mostra a vida atual e máxima do jogador.",
    ["ESP TRACERS"] = "Mostra linhas entre a tela e os jogadores.",
    ["ESP SKELETON"] = "Mostra as articulações e os membros dos jogadores.",
    ["ESP CHAMS"] = "Destaca o personagem visualmente.",
    ["CORES AUTOMÁTICAS POR TIME"] = "Usa a cor do time para identificar cada jogador.",
    ["AIMBOT"] = "Ativa ou desativa o auxílio de mira.",
    ["SHOW FOV"] = "Exibe o círculo de alcance da mira.",
    ["FOV RADIUS"] = "Altera o raio do círculo de FOV.",
    ["FOV THICKNESS"] = "Altera a espessura do círculo de FOV.",
    ["SMOOTHNESS"] = "Controla a suavidade do movimento da câmera.",
    ["WALL CHECK"] = "Ignora alvos que estejam atrás de paredes.",
    ["TEAM CHECK"] = "Evita selecionar jogadores do mesmo time.",
    ["PRIORIDADE DISTÂNCIA"] = "Prioriza o alvo mais próximo da mira em relação à distância.",
    ["DESTACAR ALVO"] = "Destaca com Highlight o jogador escolhido pelo aimbot.",
    ["HITBOX"] = "Ativa o recurso de hitbox do hub.",
    ["SPEED HACK"] = "Controla a velocidade do personagem.",
    ["DASH"] = "Move o personagem rapidamente para frente.",
    ["DESTRUIR"] = "Fecha o hub e remove seus recursos ativos.",
}

local function AdicionarDescricao(object, nome)
    local descricao = FunctionDescriptions[nome] or ("Configura a função " .. nome .. ".")
    if not object or not object.Parent then return end

    local isToggle = object:IsA("TextButton") and object.Text == ""
    if isToggle then
        local optionLabel = object:FindFirstChildOfClass("TextLabel")
        if optionLabel then
            optionLabel.Position = UDim2.fromOffset(10, 1)
            optionLabel.Size = UDim2.new(1, -55, 0, 16)
            optionLabel.TextYAlignment = Enum.TextYAlignment.Center
        end
        local switch = object:FindFirstChildOfClass("Frame")
        if switch then switch.Position = UDim2.new(1, -40, 0.5, -8) end
    elseif object:IsA("TextButton") then
        object.TextYAlignment = Enum.TextYAlignment.Top
        object.TextXAlignment = Enum.TextXAlignment.Left
        local padding = object:FindFirstChild("DescriptionPadding") or Instance.new("UIPadding")
        padding.Name = "DescriptionPadding"
        padding.PaddingTop = UDim.new(0, 3)
        padding.PaddingLeft = UDim.new(0, 10)
        padding.Parent = object
    end

    local label = object:FindFirstChild("FunctionDescription") or Instance.new("TextLabel")
    label.Name = "FunctionDescription"
    label.BackgroundTransparency = 1
    label.Position = isToggle and UDim2.fromOffset(10, 17) or UDim2.fromOffset(10, 16)
    label.Size = UDim2.new(1, -20, 0, 8)
    label.Text = descricao
    label.TextColor3 = COLORS.Muted
    label.TextSize = 6
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.TextYAlignment = Enum.TextYAlignment.Center
    label.TextTruncate = Enum.TextTruncate.AtEnd
    label.ZIndex = (object.ZIndex or 1) + 1
    label.Parent = object
end
--========================================================--
-- MAIN
--========================================================--

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Size = UDim2.fromOffset(UI_WIDTH, UI_HEIGHT)
Main.AnchorPoint = Vector2.new(0.5, 0.5)
Main.Position = UDim2.fromScale(0.5, 0.5)
Main.BackgroundColor3 = COLORS.Background
Main.BorderSizePixel = 0
Main.ClipsDescendants = true
Main.ZIndex = 100
Main.Parent = Gui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = COLORS.Stroke
MainStroke.Thickness = 1
MainStroke.Transparency = 0.1
MainStroke.Parent = Main

--========================================================--
-- LINHA SUPERIOR
--========================================================--

local TopLine = Instance.new("Frame")
TopLine.Size = UDim2.new(1, -8, 0, 2)
TopLine.Position = UDim2.fromOffset(4, 2)
TopLine.BackgroundColor3 = Accent
TopLine.BorderSizePixel = 0
TopLine.ClipsDescendants = true
TopLine.Parent = Main

local TopLineCorner = Instance.new("UICorner")
TopLineCorner.CornerRadius = UDim.new(1, 0)
TopLineCorner.Parent = TopLine

-- Efeito visual: brilho animado na linha superior
local TopGradient = Instance.new("UIGradient")
TopGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, COLORS.OliveDark),
    ColorSequenceKeypoint.new(0.5, COLORS.Green),
    ColorSequenceKeypoint.new(1, COLORS.OliveDark)
})
TopGradient.Offset = Vector2.new(-1, 0)
TopGradient.Parent = TopLine

task.spawn(function()
    while TopLine and TopLine.Parent do
        local tween = TweenService:Create(TopGradient, TweenInfo.new(2.4, Enum.EasingStyle.Linear), {Offset = Vector2.new(1, 0)})
        tween:Play()
        tween.Completed:Wait()
        TopGradient.Offset = Vector2.new(-1, 0)
    end
end)

--========================================================--
-- HEADER
--========================================================--

local Top = Instance.new("Frame")
Top.Size = UDim2.new(1, 0, 0, 44)
Top.Position = UDim2.fromOffset(0, 2)
Top.BackgroundColor3 = COLORS.Panel
Top.BorderSizePixel = 0
Top.ClipsDescendants = true
Top.Parent = Main

local TopCorner = Instance.new("UICorner")
TopCorner.CornerRadius = UDim.new(0, 10)
TopCorner.Parent = Top

local Badge = Instance.new("Frame")
Badge.Size = UDim2.fromOffset(30, 30)
Badge.Position = UDim2.fromOffset(8, 7)
Badge.BackgroundColor3 = COLORS.OliveDark
Badge.BorderSizePixel = 0
Badge.Parent = Top

local BadgeCorner = Instance.new("UICorner")
BadgeCorner.CornerRadius = UDim.new(0, 6)
BadgeCorner.Parent = Badge

local BadgeStroke = Instance.new("UIStroke")
BadgeStroke.Color = Accent
BadgeStroke.Thickness = 1
BadgeStroke.Parent = Badge

local BadgeText = Instance.new("TextLabel")
BadgeText.Size = UDim2.fromScale(1, 1)
BadgeText.BackgroundTransparency = 1
BadgeText.Text = "NX"
BadgeText.TextColor3 = COLORS.Text
BadgeText.TextSize = 11
BadgeText.Font = Enum.Font.GothamBlack
BadgeText.Parent = Badge

local Title = Instance.new("TextLabel")
Title.BackgroundTransparency = 1
Title.Position = UDim2.fromOffset(44, 5)
Title.Size = UDim2.fromOffset(80, 18)
Title.Text = "NOXYS HUB"
Title.TextColor3 = COLORS.Text
Title.TextSize = 12
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Top

local StatusDot = Instance.new("Frame")
StatusDot.Size = UDim2.fromOffset(6, 6)
StatusDot.Position = UDim2.fromOffset(128, 11)
StatusDot.BackgroundColor3 = COLORS.Green
StatusDot.BorderSizePixel = 0
StatusDot.Parent = Top

local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(1, 0)
StatusCorner.Parent = StatusDot

-- Pulso suave do indicador online
local StatusScale = Instance.new("UIScale")
StatusScale.Scale = 1
StatusScale.Parent = StatusDot
task.spawn(function()
    while StatusDot and StatusDot.Parent do
        local pulseUp = TweenService:Create(StatusScale, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), {Scale = 1.35})
        local pulseDown = TweenService:Create(StatusScale, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {Scale = 1})
        pulseUp:Play(); pulseUp.Completed:Wait()
        pulseDown:Play(); pulseDown.Completed:Wait()
    end
end)

local StatusText = Instance.new("TextLabel")
StatusText.BackgroundTransparency = 1
StatusText.Position = UDim2.fromOffset(138, 5)
StatusText.Size = UDim2.fromOffset(45, 18)
StatusText.Text = "ONLINE"
StatusText.TextColor3 = COLORS.Green
StatusText.TextSize = 7
StatusText.Font = Enum.Font.GothamBold
StatusText.TextXAlignment = Enum.TextXAlignment.Left
StatusText.Parent = Top

local Subtitle = Instance.new("TextLabel")
Subtitle.BackgroundTransparency = 1
Subtitle.Position = UDim2.fromOffset(45, 23)
Subtitle.Size = UDim2.new(1, -90, 0, 14)
Subtitle.Text = "TACTICAL SYSTEM"
Subtitle.TextColor3 = COLORS.Muted
Subtitle.TextSize = 7
Subtitle.Font = Enum.Font.GothamMedium
Subtitle.TextXAlignment = Enum.TextXAlignment.Left
Subtitle.Parent = Top

local Minimize = Instance.new("TextButton")
Minimize.Size = UDim2.fromOffset(26, 26)
Minimize.Position = UDim2.new(1, -34, 0, 9)
Minimize.BackgroundColor3 = COLORS.Panel2
Minimize.Text = "—"
Minimize.TextColor3 = COLORS.Text
Minimize.TextSize = 13
Minimize.Font = Enum.Font.GothamBold
Minimize.AutoButtonColor = false
Minimize.Parent = Top

local MinCorner = Instance.new("UICorner")
MinCorner.CornerRadius = UDim.new(0, 6)
MinCorner.Parent = Minimize

--========================================================--
-- EFEITOS DECORATIVOS
--========================================================--

local ParticleLayer = Instance.new("Frame")
ParticleLayer.Name = "ParticleLayer"
ParticleLayer.Size = UDim2.fromScale(1, 1)
ParticleLayer.BackgroundTransparency = 1
ParticleLayer.ClipsDescendants = true
ParticleLayer.ZIndex = 1
ParticleLayer.Parent = Main

local ParticleRandom = Random.new()
local function CriarParticula()
    if not ParticleLayer or not ParticleLayer.Parent then return end
    local particle = Instance.new("Frame")
    local size = ParticleRandom:NextInteger(1, 3)
    particle.Size = UDim2.fromOffset(size, size)
    particle.Position = UDim2.new(ParticleRandom:NextNumber(0.02, 0.98), 0, 1.05, 0)
    particle.BackgroundColor3 = ParticleRandom:NextNumber() > 0.5 and COLORS.Olive or COLORS.Green
    particle.BackgroundTransparency = 0.45
    particle.BorderSizePixel = 0
    particle.ZIndex = 1
    particle.Parent = ParticleLayer

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = particle

    local destino = UDim2.new(
        particle.Position.X.Scale + ParticleRandom:NextNumber(-0.08, 0.08), 0,
        -0.08, 0
    )
    local duracao = ParticleRandom:NextNumber(4, 7)
    local tween = TweenService:Create(particle, TweenInfo.new(duracao, Enum.EasingStyle.Linear), {
        Position = destino,
        BackgroundTransparency = 1
    })
    tween:Play()
    tween.Completed:Connect(function()
        if particle then particle:Destroy() end
    end)
end

task.spawn(function()
    while ParticleLayer and ParticleLayer.Parent do
        CriarParticula()
        task.wait(0.35)
    end
end)

--========================================================--
-- ABAS
--========================================================--

local Tabs = Instance.new("Frame")
Tabs.Size = UDim2.new(1, -12, 0, 30)
Tabs.Position = UDim2.fromOffset(6, 52)
Tabs.BackgroundColor3 = COLORS.Panel
Tabs.BorderSizePixel = 0
Tabs.Parent = Main

local TabsCorner = Instance.new("UICorner")
TabsCorner.CornerRadius = UDim.new(0, 6)
TabsCorner.Parent = Tabs

local TabLayout = Instance.new("UIListLayout")
TabLayout.FillDirection = Enum.FillDirection.Horizontal
TabLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
TabLayout.VerticalAlignment = Enum.VerticalAlignment.Center
TabLayout.Padding = UDim.new(0, 2)
TabLayout.Parent = Tabs

local Pages = {}
local TabButtons = {}

--========================================================--
-- CRIAR PÁGINA
--========================================================--

local function CreatePage(Name)
	local Page = Instance.new("ScrollingFrame")
	Page.Name = Name
	Page.Size = UDim2.new(1, -12, 1, -90)
	Page.Position = UDim2.fromOffset(6, 86)
	Page.BackgroundTransparency = 1
	Page.BorderSizePixel = 0
	Page.ScrollBarThickness = 2
	Page.ScrollBarImageColor3 = Accent
	Page.ScrollBarImageTransparency = 0.15
	Page.AutomaticCanvasSize = Enum.AutomaticSize.Y
	Page.CanvasSize = UDim2.new()
	Page.Visible = false
	Page.Parent = Main

	local Layout = Instance.new("UIListLayout")
	Layout.Padding = UDim.new(0, 6)
	Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
	Layout.SortOrder = Enum.SortOrder.LayoutOrder
	Layout.Parent = Page

	Pages[Name] = Page
	return Page
end

--========================================================--
-- CRIAR ABA
--========================================================--

local function CreateTab(Name, Icon)
	local Button = Instance.new("TextButton")
	Button.Size = UDim2.fromOffset(52, 22)
	Button.BackgroundColor3 = COLORS.Panel2
	Button.Text = Icon .. " " .. Name
	Button.TextColor3 = COLORS.Muted
	Button.TextSize = 7
	Button.Font = Enum.Font.GothamBold
	Button.AutoButtonColor = false
	Button.Parent = Tabs

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 5)
	Corner.Parent = Button

	TabButtons[Name] = Button

	Button.MouseButton1Click:Connect(function()
		for TabName, Page in pairs(Pages) do
			Page.Visible = TabName == Name
			local Tab = TabButtons[TabName]
			if TabName == Name then
				Tab.BackgroundColor3 = Accent
				Tab.TextColor3 = COLORS.Text
			else
				Tab.BackgroundColor3 = COLORS.Panel2
				Tab.TextColor3 = COLORS.Muted
			end
		end
	end)
end

CreatePage("Main")
CreatePage("Combat")
CreatePage("Visual")
CreatePage("Movement")
CreatePage("Config")

CreateTab("Main", "●")
CreateTab("Combat", "◉")
CreateTab("Visual", "◆")
CreateTab("Movement", "■")
CreateTab("Config", "▲")

--========================================================--
-- SECTION (COM CONTAINER PRÓPRIO - CORRIGIDO)
--========================================================--

local SectionContainers = {}

local function Section(Page, Text)
    local PageName = Page.Name
    
    if not SectionContainers[PageName] then
        SectionContainers[PageName] = {}
    end
    
    local SectionOrder = (#SectionContainers[PageName] + 1) * 100
    
    local Container = Instance.new("Frame")
    Container.Size = UDim2.new(1, -4, 0, 0)
    Container.AutomaticSize = Enum.AutomaticSize.Y
    Container.BackgroundTransparency = 1
    Container.BorderSizePixel = 0
    Container.LayoutOrder = SectionOrder
    Container.Parent = Page

    local ContainerCorner = Instance.new("UICorner")
    ContainerCorner.CornerRadius = UDim.new(0, 8)
    ContainerCorner.Parent = Container

    local ContainerLayout = Instance.new("UIListLayout")
    ContainerLayout.Padding = UDim.new(0, 4)
    ContainerLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    ContainerLayout.SortOrder = Enum.SortOrder.LayoutOrder
    ContainerLayout.Parent = Container
    
    local Header = Instance.new("Frame")
    Header.Size = UDim2.new(1, 0, 0, 28)
    Header.BackgroundColor3 = COLORS.Panel2
    Header.BackgroundTransparency = 0.18
    Header.BorderSizePixel = 0
    Header.ClipsDescendants = true
    Header.LayoutOrder = 0
    Header.Parent = Container

    local HeaderGradient = Instance.new("UIGradient")
    HeaderGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, COLORS.Panel2),
        ColorSequenceKeypoint.new(1, COLORS.OliveDark)
    })
    HeaderGradient.Transparency = NumberSequence.new({
        NumberSequenceKeypoint.new(0, 0.05),
        NumberSequenceKeypoint.new(1, 0.45)
    })
    HeaderGradient.Parent = Header

    local HeaderAccent = Instance.new("Frame")
    HeaderAccent.Size = UDim2.fromOffset(3, 16)
    HeaderAccent.Position = UDim2.fromOffset(7, 6)
    HeaderAccent.BackgroundColor3 = Accent
    HeaderAccent.BorderSizePixel = 0
    HeaderAccent.Parent = Header

    local HeaderAccentCorner = Instance.new("UICorner")
    HeaderAccentCorner.CornerRadius = UDim.new(1, 0)
    HeaderAccentCorner.Parent = HeaderAccent

    local HeaderDot = Instance.new("Frame")
    HeaderDot.Size = UDim2.fromOffset(5, 5)
    HeaderDot.Position = UDim2.new(1, -14, 0.5, -2)
    HeaderDot.BackgroundColor3 = Accent
    HeaderDot.BorderSizePixel = 0
    HeaderDot.Parent = Header

    local HeaderDotCorner = Instance.new("UICorner")
    HeaderDotCorner.CornerRadius = UDim.new(1, 0)
    HeaderDotCorner.Parent = HeaderDot
    
    local HeaderCorner = Instance.new("UICorner")
    HeaderCorner.CornerRadius = UDim.new(0, 8)
    HeaderCorner.Parent = Header
    
    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(1, -34, 1, 0)
    Label.Position = UDim2.fromOffset(16, 0)
    Label.BackgroundTransparency = 1
    Label.Text = string.upper(Text)
    Label.TextColor3 = COLORS.Text
    Label.TextSize = 8
    Label.Font = Enum.Font.GothamBold
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Header

    local HeaderLine = Instance.new("Frame")
    HeaderLine.Size = UDim2.new(1, -28, 0, 1)
    HeaderLine.Position = UDim2.new(0, 16, 1, -3)
    HeaderLine.BackgroundColor3 = Accent
    HeaderLine.BackgroundTransparency = 0.55
    HeaderLine.BorderSizePixel = 0
    HeaderLine.Parent = Header

    -- Brilho e partículas discretas passando no cabeçalho da seção
    local SectionShine = Instance.new("Frame")
    SectionShine.Size = UDim2.new(0, 2, 1, 0)
    SectionShine.Position = UDim2.new(0, -8, 0, 0)
    SectionShine.Rotation = 15
    SectionShine.BackgroundColor3 = COLORS.Text
    SectionShine.BackgroundTransparency = 0.78
    SectionShine.BorderSizePixel = 0
    SectionShine.ZIndex = 2
    SectionShine.Parent = Header

    task.spawn(function()
        while SectionShine and SectionShine.Parent do
            local shineTween = TweenService:Create(SectionShine, TweenInfo.new(1.8, Enum.EasingStyle.Linear), {Position = UDim2.new(1, 8, 0, 0)})
            shineTween:Play()
            shineTween.Completed:Wait()
            SectionShine.Position = UDim2.new(0, -8, 0, 0)
            task.wait(1.4)
        end
    end)

    task.spawn(function()
        local random = Random.new()
        while Header and Header.Parent do
            local dot = Instance.new("Frame")
            local dotSize = random:NextInteger(1, 2)
            dot.Size = UDim2.fromOffset(dotSize, dotSize)
            dot.Position = UDim2.new(random:NextNumber(0.15, 0.92), 0, random:NextNumber(0.25, 0.8), 0)
            dot.BackgroundColor3 = random:NextNumber() > 0.5 and Accent or COLORS.Green
            dot.BackgroundTransparency = 0.25
            dot.BorderSizePixel = 0
            dot.ZIndex = 2
            dot.Parent = Header
            local dotCorner = Instance.new("UICorner")
            dotCorner.CornerRadius = UDim.new(1, 0)
            dotCorner.Parent = dot
            local fade = TweenService:Create(dot, TweenInfo.new(random:NextNumber(0.8, 1.5), Enum.EasingStyle.Sine), {
                Position = UDim2.new(dot.Position.X.Scale + random:NextNumber(-0.08, 0.08), 0, dot.Position.Y.Scale - 0.2, 0),
                BackgroundTransparency = 1
            })
            fade:Play()
            fade.Completed:Connect(function()
                if dot then dot:Destroy() end
            end)
            task.wait(0.55)
        end
    end)
    
    table.insert(SectionContainers[PageName], Container)
    
    return Container
end

--========================================================--
-- TOGGLE (DENTRO DO CONTAINER)
--========================================================--

local function Toggle(Container, Text, Default, Callback)
	local State = Default
	local Button = Instance.new("TextButton")
	Button.Size = UDim2.new(1, 0, 0, 32)
	Button.BackgroundColor3 = COLORS.Panel
	Button.BorderSizePixel = 0
	Button.Text = ""
	Button.AutoButtonColor = false
	Button.LayoutOrder = 1
	Button.Parent = Container

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 6)
	Corner.Parent = Button

	local Label = Instance.new("TextLabel")
	Label.BackgroundTransparency = 1
	Label.Position = UDim2.fromOffset(10, 0)
	Label.Size = UDim2.new(1, -55, 1, 0)
	Label.Text = Text
	Label.TextColor3 = COLORS.Text
	Label.TextSize = 9
	Label.Font = Enum.Font.GothamMedium
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Button

	local Switch = Instance.new("Frame")
	Switch.Size = UDim2.fromOffset(30, 16)
	Switch.Position = UDim2.new(1, -40, 0.5, -8)
	Switch.BackgroundColor3 = COLORS.OliveDark
	Switch.Parent = Button

	local SwitchCorner = Instance.new("UICorner")
	SwitchCorner.CornerRadius = UDim.new(1, 0)
	SwitchCorner.Parent = Switch

	local Circle = Instance.new("Frame")
	Circle.Size = UDim2.fromOffset(12, 12)
	Circle.Position = UDim2.fromOffset(2, 2)
	Circle.BackgroundColor3 = COLORS.Text
	Circle.BorderSizePixel = 0
	Circle.Parent = Switch

	local CircleCorner = Instance.new("UICorner")
	CircleCorner.CornerRadius = UDim.new(1, 0)
	CircleCorner.Parent = Circle

	local function Update()
		local SwitchColor = State and Accent or COLORS.OliveDark
		local CirclePosition = State and UDim2.new(1, -14, 0, 2) or UDim2.fromOffset(2, 2)
		TweenService:Create(Switch, TweenInfo.new(0.12), {BackgroundColor3 = SwitchColor}):Play()
		TweenService:Create(Circle, TweenInfo.new(0.12), {Position = CirclePosition}):Play()
		if Callback then Callback(State) end
	end

	Button.MouseButton1Click:Connect(function()
		State = not State
		Update()
	end)

	Update()
    AdicionarDescricao(Button, Text)
	return {Set = function(Value) State = Value; Update() end}
end

--========================================================--
-- SLIDER (DENTRO DO CONTAINER)
--========================================================--

local function Slider(Container, Text, Min, Max, Default, Callback)
	local Value = Default
	local Dragging = false
	local Frame = Instance.new("Frame")
	Frame.Size = UDim2.new(1, 0, 0, 42)
	Frame.BackgroundColor3 = COLORS.Panel
	Frame.BorderSizePixel = 0
	Frame.LayoutOrder = 1
	Frame.Parent = Container

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 6)
	Corner.Parent = Frame

	local Label = Instance.new("TextLabel")
	Label.BackgroundTransparency = 1
	Label.Position = UDim2.fromOffset(10, 3)
	Label.Size = UDim2.new(1, -60, 0, 14)
	Label.Text = Text
	Label.TextColor3 = COLORS.Text
	Label.TextSize = 8
	Label.Font = Enum.Font.GothamMedium
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Frame

	local ValueLabel = Instance.new("TextLabel")
	ValueLabel.BackgroundTransparency = 1
	ValueLabel.Position = UDim2.new(1, -45, 0, 3)
	ValueLabel.Size = UDim2.fromOffset(35, 14)
	ValueLabel.Text = tostring(Value)
	ValueLabel.TextColor3 = Accent
	ValueLabel.TextSize = 8
	ValueLabel.Font = Enum.Font.GothamBold
	ValueLabel.Parent = Frame

	local Bar = Instance.new("Frame")
	Bar.Size = UDim2.new(1, -20, 0, 4)
	Bar.Position = UDim2.new(0, 10, 1, -10)
	Bar.BackgroundColor3 = COLORS.OliveDark
	Bar.BorderSizePixel = 0
	Bar.Parent = Frame

	local BarCorner = Instance.new("UICorner")
	BarCorner.CornerRadius = UDim.new(1, 0)
	BarCorner.Parent = Bar

	local Percent = math.clamp((Value - Min) / (Max - Min), 0, 1)

	local Fill = Instance.new("Frame")
	Fill.Size = UDim2.new(Percent, 0, 1, 0)
	Fill.BackgroundColor3 = Accent
	Fill.BorderSizePixel = 0
	Fill.Parent = Bar

	local FillCorner = Instance.new("UICorner")
	FillCorner.CornerRadius = UDim.new(1, 0)
	FillCorner.Parent = Fill

	local Knob = Instance.new("Frame")
	Knob.Size = UDim2.fromOffset(9, 9)
	Knob.AnchorPoint = Vector2.new(0.5, 0.5)
	Knob.Position = UDim2.new(Percent, 0, 0.5, 0)
	Knob.BackgroundColor3 = COLORS.Text
	Knob.BorderSizePixel = 0
	Knob.Parent = Bar

	local KnobCorner = Instance.new("UICorner")
	KnobCorner.CornerRadius = UDim.new(1, 0)
	KnobCorner.Parent = Knob

	local function SetValue(X)
		local Width = Bar.AbsoluteSize.X
		if Width <= 0 then return end
		local P = math.clamp((X - Bar.AbsolutePosition.X) / Width, 0, 1)
		Value = math.floor(Min + (Max - Min) * P + 0.5)
		Fill.Size = UDim2.new(P, 0, 1, 0)
		Knob.Position = UDim2.new(P, 0, 0.5, 0)
		ValueLabel.Text = tostring(Value)
		if Callback then Callback(Value) end
	end

	Bar.InputBegan:Connect(function(Input)
		if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
			Dragging = true
			SetValue(Input.Position.X)
		end
	end)

	UserInputService.InputChanged:Connect(function(Input)
		if not Dragging then return end
		if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseMovement then
			SetValue(Input.Position.X)
		end
	end)

	UserInputService.InputEnded:Connect(function(Input)
		if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
			Dragging = false
		end
	end)

	return Frame
end

--========================================================--
-- BOTÃO (DENTRO DO CONTAINER)
--========================================================--

local function Button(Container, Text, Callback)
	local B = Instance.new("TextButton")
	B.Size = UDim2.new(1, 0, 0, 30)
	B.BackgroundColor3 = COLORS.Panel
	B.BorderSizePixel = 0
	B.Text = Text
	B.TextColor3 = COLORS.Text
	B.TextSize = 9
	B.Font = Enum.Font.GothamMedium
	B.AutoButtonColor = false
	B.LayoutOrder = 1
	B.Parent = Container

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 6)
	Corner.Parent = B

	B.MouseButton1Click:Connect(function()
		if Callback then Callback() end
	end)

    AdicionarDescricao(B, Text)
	return B
end

--========================================================--
-- INPUT (DENTRO DO CONTAINER)
--========================================================--

local function Input(Container, LabelText, Placeholder, Callback)
    local Frame = Instance.new("Frame")
    Frame.Size = UDim2.new(1, 0, 0, 38)
    Frame.BackgroundColor3 = COLORS.Panel
    Frame.BorderSizePixel = 0
    Frame.LayoutOrder = 1
    Frame.Parent = Container

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 6)
    Corner.Parent = Frame

    local Label = Instance.new("TextLabel")
    Label.BackgroundTransparency = 1
    Label.Position = UDim2.fromOffset(10, 3)
    Label.Size = UDim2.new(1, -20, 0, 14)
    Label.Text = LabelText
    Label.TextColor3 = COLORS.Text
    Label.TextSize = 8
    Label.Font = Enum.Font.GothamMedium
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Frame

    local Box = Instance.new("TextBox")
    Box.Size = UDim2.new(1, -20, 0, 18)
    Box.Position = UDim2.fromOffset(10, 18)
    Box.BackgroundColor3 = COLORS.Panel2
    Box.BorderSizePixel = 0
    Box.Text = Placeholder or ""
    Box.TextColor3 = COLORS.Text
    Box.PlaceholderText = Placeholder or ""
    Box.PlaceholderColor3 = COLORS.Muted
    Box.TextSize = 8
    Box.Font = Enum.Font.Code
    Box.Parent = Frame

    local BoxCorner = Instance.new("UICorner")
    BoxCorner.CornerRadius = UDim.new(0, 4)
    BoxCorner.Parent = Box

    Box.FocusLost:Connect(function(enterPressed)
        if enterPressed and Callback then
            Callback(Box.Text)
        end
    end)

    return Box
end

--========================================================--
-- COLOR PICKER (MODERNO)
--========================================================--

local function ColorPicker(Container, Category, DefaultColor)
    local colorList = {
        {Name = "Branco", Color = Color3.fromRGB(255,255,255)},
        {Name = "Vermelho", Color = Color3.fromRGB(255,50,50)},
        {Name = "Azul", Color = Color3.fromRGB(50,150,255)},
        {Name = "Verde", Color = Color3.fromRGB(50,255,100)},
        {Name = "Amarelo", Color = Color3.fromRGB(255,220,50)},
        {Name = "Roxo", Color = Color3.fromRGB(170,0,255)},
        {Name = "Ciano", Color = Color3.fromRGB(0,255,255)},
        {Name = "Rosa", Color = Color3.fromRGB(255,0,180)},
    }

    local Holder = Instance.new("Frame")
    Holder.Size = UDim2.new(1, 0, 0, 28)
    Holder.BackgroundTransparency = 1
    Holder.BorderSizePixel = 0
    Holder.LayoutOrder = 1
    Holder.Parent = Container

    local Layout = Instance.new("UIListLayout")
    Layout.FillDirection = Enum.FillDirection.Horizontal
    Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    Layout.VerticalAlignment = Enum.VerticalAlignment.Center
    Layout.Padding = UDim.new(0, 6)
    Layout.Parent = Holder

    local selectedButton = nil

    local function updateSelection(button)
        if selectedButton then
            selectedButton.UIStroke.Transparency = 1
        end
        selectedButton = button
        button.UIStroke.Transparency = 0
    end

    for _, col in ipairs(colorList) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.fromOffset(22, 22)
        btn.BackgroundColor3 = col.Color
        btn.BorderSizePixel = 0
        btn.AutoButtonColor = false
        btn.Text = ""
        btn.Parent = Holder

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(1, 0)
        corner.Parent = btn

        local stroke = Instance.new("UIStroke")
        stroke.Color = Color3.fromRGB(255,255,255)
        stroke.Thickness = 2
        stroke.Transparency = 1
        stroke.Parent = btn

        btn.MouseButton1Click:Connect(function()
            if Category == "FOV" then
                Aimbot_CorFOV = col.Color
                FOVCircle.Color = col.Color
            elseif Category == "Hitbox" then
                Hitbox_Cor = col.Color
            elseif Category == "ESP" then
                CorESP = col.Color
                AtualizarESP()
            end
            updateSelection(btn)
            Notify(Category .. " cor: " .. col.Name)
        end)

        if col.Color == DefaultColor then
            updateSelection(btn)
        end
    end
end

--========================================================--
-- NOTIFICAÇÕES
--========================================================--

local NotificationHolder = Instance.new("Frame")
NotificationHolder.Size = UDim2.fromOffset(180, 130)
NotificationHolder.Position = UDim2.new(1, -190, 1, -140)
NotificationHolder.BackgroundTransparency = 1
NotificationHolder.ZIndex = 200
NotificationHolder.Parent = Gui

local NotifyLayout = Instance.new("UIListLayout")
NotifyLayout.VerticalAlignment = Enum.VerticalAlignment.Bottom
NotifyLayout.Padding = UDim.new(0, 4)
NotifyLayout.Parent = NotificationHolder

local function Notify(Text)
	if not Config.Notifications then return end
	local N = Instance.new("Frame")
	N.Size = UDim2.new(1, 0, 0, 32)
	N.BackgroundColor3 = COLORS.Panel
	N.BorderSizePixel = 0
	N.ZIndex = 201
	N.Parent = NotificationHolder

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 6)
	Corner.Parent = N

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Accent
	Stroke.Thickness = 1
	Stroke.Transparency = 0.25
	Stroke.Parent = N

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(1, -12, 1, 0)
	Label.Position = UDim2.fromOffset(6, 0)
	Label.BackgroundTransparency = 1
	Label.Text = Text
	Label.TextColor3 = COLORS.Text
	Label.TextSize = 8
	Label.Font = Enum.Font.GothamMedium
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = N

	N.Position = UDim2.new(1, 8, 0, 0)
	TweenService:Create(N, TweenInfo.new(0.18, Enum.EasingStyle.Quart), {Position = UDim2.new(0, 0, 0, 0)}):Play()

	task.delay(1.8, function()
		local Tween = TweenService:Create(N, TweenInfo.new(0.18, Enum.EasingStyle.Quart), {Position = UDim2.new(1, 8, 0, 0)})
		Tween:Play()
		Tween.Completed:Wait()
		N:Destroy()
	end)
end

--========================================================--
-- FUNÇÕES ESP
--========================================================--

local function GetPlayerTime(player)
    if player.Team then
        local teamName = player.Team.Name
        for time, _ in pairs(TimesMarcados) do
            if teamName:find(time:gsub("%[", ""):gsub("%]", ""), 1, true) then return time end
        end
    end
    return nil
end

local function GetESPColor(player)
    if ESP_CoresPorTime and player.Team and player.Team.TeamColor then
        return player.Team.TeamColor.Color
    end
    return CorESP
end

local function AtualizarESP()
    if not CorESP then CorESP = Color3.fromRGB(255, 50, 50) end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player and p.Character then
            local mostrarESP = true
            if ESP_TimesAtivado then
                local timePlayer = GetPlayerTime(p)
                if timePlayer then mostrarESP = TimesMarcados[timePlayer] or false else mostrarESP = false end
            end
            local espColor = GetESPColor(p)
            local h = p.Character:FindFirstChild("NoahzinESP")
            if h then h.Enabled = ESP_GlobalAtivado and ESP_Chams and mostrarESP; pcall(function() h.FillColor = espColor end) end
            local head = p.Character:FindFirstChild("Head")
            if head then
                local nameGui = head:FindFirstChild("NoahzinName")
                if nameGui then nameGui.Enabled = ESP_GlobalAtivado and ESP_Names and mostrarESP
                    if nameGui:FindFirstChild("TextLabel") then pcall(function() nameGui.TextLabel.TextColor3 = espColor end) end
                end
                -- Nome do time
                local timeGui = head:FindFirstChild("NoahzinTime")
                if timeGui then
                    timeGui.Enabled = ESP_GlobalAtivado and ESP_TimesNomes and mostrarESP
                    local timeLabel = timeGui:FindFirstChild("TextLabel")
                    if timeLabel then
                        timeLabel.Text = p.Team and p.Team.Name or "SEM TIME"
                        pcall(function() timeLabel.TextColor3 = espColor end)
                    end
                end
                -- Distância
                local distanciaGui = head:FindFirstChild("NoahzinDistancia")
                if distanciaGui then
                    distanciaGui.Enabled = ESP_GlobalAtivado and ESP_Distancia and mostrarESP
                    local distanciaLabel = distanciaGui:FindFirstChild("TextLabel")
                    local meuHead = Player.Character and Player.Character:FindFirstChild("Head")
                    if distanciaLabel and meuHead then
                        distanciaLabel.Text = string.format("[%dm]", math.floor((head.Position - meuHead.Position).Magnitude))
                        pcall(function() distanciaLabel.TextColor3 = espColor end)
                    end
                end
                -- Vida
                local vidaGui = head:FindFirstChild("NoahzinVida")
                if vidaGui then
                    vidaGui.Enabled = ESP_GlobalAtivado and ESP_Vida and mostrarESP
                    if vidaGui:FindFirstChild("TextLabel") then
                        local hum = p.Character:FindFirstChildOfClass("Humanoid")
                        if hum then
                            vidaGui.TextLabel.Text = string.format("[%d/%d]", math.floor(hum.Health), math.floor(hum.MaxHealth))
                        end
                    end
                end
            end
            if LinhasTracers[p] then pcall(function() LinhasTracers[p].Color = GetESPColor(p) end); LinhasTracers[p].Visible = ESP_GlobalAtivado and ESP_Tracers and mostrarESP end
        end
    end
end

local function CriarCaixaESP2D(p)
    if p == Player or CaixasESP2D[p] then return end
    local ok, box = pcall(function() return Drawing.new("Square") end)
    if ok and box then
        box.Visible = false; box.Color = CorESP; box.Thickness = ESP_BoxThickness; box.Filled = false
        CaixasESP2D[p] = box
    end
end
local function RemoverCaixaESP2D(p)
    local box = CaixasESP2D[p]
    if box then pcall(function() if type(box.Remove) == "function" then box:Remove() elseif type(box.Destroy) == "function" then box:Destroy() end end); CaixasESP2D[p] = nil end
end
local function AtualizarCaixasESP2D(camera)
    for p, box in pairs(CaixasESP2D) do
        local c=p.Character; local root=c and c:FindFirstChild("HumanoidRootPart"); local hum=c and c:FindFirstChildOfClass("Humanoid")
        local mostrar=ESP_GlobalAtivado and ESP_Box
        if ESP_TimesAtivado then local t=c and GetPlayerTime(p); mostrar=mostrar and t ~= nil and TimesMarcados[t] == true end
        if not root or not hum or hum.Health <= 0 or not mostrar or (camera.CFrame.Position-root.Position).Magnitude > ESP_BoxMaxDistance then box.Visible=false
        else
            local cf,size=c:GetBoundingBox(); local a=cf.Position+Vector3.new(0,size.Y/2,0); local b=cf.Position-Vector3.new(0,size.Y/2,0)
            local ap,av=camera:WorldToViewportPoint(a); local bp,bv=camera:WorldToViewportPoint(b)
            if av or bv then local h=math.max(math.abs(bp.Y-ap.Y),2); local w=h*0.55; box.Size=Vector2.new(w,h); box.Position=Vector2.new(ap.X-w/2,ap.Y); box.Color=GetESPColor(p); box.Thickness=ESP_BoxThickness; box.Visible=true else box.Visible=false end
        end
    end
end

local EsqueletosESP = {}
local SkeletonR6 = {
    {"Head", "Torso"},
    {"Torso", "Left Arm"},
    {"Torso", "Right Arm"},
    {"Torso", "Left Leg"},
    {"Torso", "Right Leg"},
}
local SkeletonR15 = {
    {"Head", "UpperTorso"},
    {"UpperTorso", "LowerTorso"},
    {"UpperTorso", "LeftUpperArm"},
    {"LeftUpperArm", "LeftLowerArm"},
    {"LeftLowerArm", "LeftHand"},
    {"UpperTorso", "RightUpperArm"},
    {"RightUpperArm", "RightLowerArm"},
    {"RightLowerArm", "RightHand"},
    {"LowerTorso", "LeftUpperLeg"},
    {"LeftUpperLeg", "LeftLowerLeg"},
    {"LeftLowerLeg", "LeftFoot"},
    {"LowerTorso", "RightUpperLeg"},
    {"RightUpperLeg", "RightLowerLeg"},
    {"RightLowerLeg", "RightFoot"},
}

local function RemoverSkeleton(p)
    local linhas = EsqueletosESP[p]
    if not linhas then return end
    for _, item in ipairs(linhas) do
        pcall(function() item.Drawing:Remove() end)
    end
    EsqueletosESP[p] = nil
end

local function CriarSkeleton(p, character)
    RemoverSkeleton(p)
    if not character then return end
    local conexoes
    if character:FindFirstChild("UpperTorso") then
        conexoes = SkeletonR15
    elseif character:FindFirstChild("Torso") then
        conexoes = SkeletonR6
    else
        return
    end
    local linhas = {}
    for _, pair in ipairs(conexoes) do
        local ok, line = pcall(function()
            local drawingLine = Drawing.new("Line")
            drawingLine.Visible = false
            drawingLine.Color = CorESP
            drawingLine.Thickness = 1.5
            drawingLine.Transparency = 1
            return drawingLine
        end)
        if ok and line then
            linhas[#linhas + 1] = {Drawing = line, Part0 = pair[1], Part1 = pair[2]}
        end
    end
    EsqueletosESP[p] = linhas
end

local function AtualizarSkeleton(camera)
    for p, linhas in pairs(EsqueletosESP) do
        local character = p.Character
        local root = character and character:FindFirstChild("HumanoidRootPart")
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        local mostrar = ESP_GlobalAtivado and ESP_Skeleton
        if ESP_TimesAtivado then
            local timePlayer = character and GetPlayerTime(p)
            mostrar = mostrar and timePlayer ~= nil and TimesMarcados[timePlayer] == true
        end
        local myRoot = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
        local distance = (myRoot and root) and (myRoot.Position - root.Position).Magnitude or math.huge
        local valido = mostrar and character and root and humanoid and humanoid.Health > 0 and distance <= ESP_BoxMaxDistance
        for _, item in ipairs(linhas) do
            local line = item.Drawing
            line.Color = GetESPColor(p)
            line.Thickness = 1.5
            line.Transparency = 1
            if not valido then
                line.Visible = false
                continue
            end
            local part0 = character:FindFirstChild(item.Part0)
            local part1 = character:FindFirstChild(item.Part1)
            if not part0 or not part1 then
                line.Visible = false
                continue
            end
            local pos0, visible0 = camera:WorldToViewportPoint(part0.Position)
            local pos1, visible1 = camera:WorldToViewportPoint(part1.Position)
            if visible0 and visible1 and pos0.Z > 0 and pos1.Z > 0 then
                line.From = Vector2.new(pos0.X, pos0.Y)
                line.To = Vector2.new(pos1.X, pos1.Y)
                line.Visible = true
            else
                line.Visible = false
            end
        end
    end
end

local function CriarESP(p)
    if p == Player then return end
    CriarCaixaESP2D(p)
    local function AdicionarUI(c)
        local head = c:WaitForChild("Head", 5)
        if not head then return end
        pcall(function() CriarSkeleton(p, c) end)
        local h = c:FindFirstChild("NoahzinESP") or Instance.new("Highlight")
        h.Name = "NoahzinESP"; pcall(function() h.FillColor = GetESPColor(p) end)
        h.OutlineColor = Color3.new(1,1,1); h.FillTransparency = 0.5; h.Enabled = ESP_GlobalAtivado and ESP_Chams; h.Parent = c
        local bg = head:FindFirstChild("NoahzinName") or Instance.new("BillboardGui")
        bg.Name = "NoahzinName"; bg.Size = UDim2.new(0, 100, 0, 20)
        bg.AlwaysOnTop = true; bg.ExtentsOffset = Vector3.new(0, 3, 0)
        bg.Enabled = ESP_GlobalAtivado and ESP_Names; bg.Parent = head
        local tl = bg:FindFirstChild("TextLabel") or Instance.new("TextLabel")
        tl.Size = UDim2.new(1, 0, 1, 0); tl.BackgroundTransparency = 1; tl.Text = p.Name
        pcall(function() tl.TextColor3 = GetESPColor(p) end)
        tl.Font = Enum.Font.GothamBold; tl.TextSize = 12; tl.TextStrokeTransparency = 0; tl.Parent = bg
        -- Nome do time
        local bgTime = head:FindFirstChild("NoahzinTime") or Instance.new("BillboardGui")
        bgTime.Name = "NoahzinTime"
        bgTime.Size = UDim2.new(0, 150, 0, 20)
        bgTime.AlwaysOnTop = true
        bgTime.ExtentsOffset = Vector3.new(0, 7, 0)
        bgTime.Enabled = ESP_GlobalAtivado and ESP_TimesNomes
        bgTime.Parent = head

        local tlTime = bgTime:FindFirstChild("TextLabel") or Instance.new("TextLabel")
        tlTime.Size = UDim2.new(1, 0, 1, 0)
        tlTime.BackgroundTransparency = 1
        tlTime.Text = p.Team and p.Team.Name or "SEM TIME"
        pcall(function() tlTime.TextColor3 = GetESPColor(p) end)
        tlTime.Font = Enum.Font.GothamBold
        tlTime.TextSize = 11
        tlTime.TextStrokeTransparency = 0
        tlTime.Parent = bgTime
        -- Vida
        local bgVida = head:FindFirstChild("NoahzinVida") or Instance.new("BillboardGui")
        bgVida.Name = "NoahzinVida"
        bgVida.Size = UDim2.new(0, 100, 0, 20)
        bgVida.AlwaysOnTop = true
        bgVida.ExtentsOffset = Vector3.new(0, 5, 0)
        bgVida.Enabled = ESP_GlobalAtivado and ESP_Vida
        bgVida.Parent = head

        local tlVida = bgVida:FindFirstChild("TextLabel") or Instance.new("TextLabel")
        tlVida.Size = UDim2.new(1, 0, 1, 0)
        tlVida.BackgroundTransparency = 1
        tlVida.Text = ""
        tlVida.TextColor3 = Color3.fromRGB(255, 255, 255)
        tlVida.Font = Enum.Font.GothamBold
        tlVida.TextSize = 11
        tlVida.TextStrokeTransparency = 0
        tlVida.Parent = bgVida
        local bgDistancia = head:FindFirstChild("NoahzinDistancia") or Instance.new("BillboardGui")
        bgDistancia.Name = "NoahzinDistancia"
        bgDistancia.Size = UDim2.new(0, 100, 0, 20)
        bgDistancia.AlwaysOnTop = true
        bgDistancia.ExtentsOffset = Vector3.new(0, 9, 0)
        bgDistancia.Enabled = ESP_GlobalAtivado and ESP_Distancia
        bgDistancia.Parent = head
        local tlDistancia = bgDistancia:FindFirstChild("TextLabel") or Instance.new("TextLabel")
        tlDistancia.Size = UDim2.new(1, 0, 1, 0)
        tlDistancia.BackgroundTransparency = 1
        tlDistancia.Text = "[0m]"
        pcall(function() tlDistancia.TextColor3 = GetESPColor(p) end)
        tlDistancia.Font = Enum.Font.GothamBold
        tlDistancia.TextSize = 11
        tlDistancia.TextStrokeTransparency = 0
        tlDistancia.Parent = bgDistancia
        if not LinhasTracers[p] then
            local tracer = Drawing.new("Line"); tracer.Visible = false
            pcall(function() tracer.Color = CorESP end); tracer.Thickness = 1.2; LinhasTracers[p] = tracer
        end
        task.spawn(AtualizarESP)
    end
    if p.Character then AdicionarUI(p.Character) end
    p.CharacterAdded:Connect(AdicionarUI)
end

for _, p in pairs(Players:GetPlayers()) do CriarESP(p) end
Players.PlayerAdded:Connect(CriarESP)
Players.PlayerRemoving:Connect(function(p) if LinhasTracers[p] then LinhasTracers[p]:Destroy(); LinhasTracers[p] = nil end; RemoverCaixaESP2D(p); RemoverSkeleton(p) end)

local function CalcularOffset3D_Dinamico(camera, alvoPosicao)
    local distancia = (alvoPosicao - camera.CFrame.Position).Magnitude
    local fovVertical = math.rad(camera.FieldOfView)
    local alturaTelaStuds = 2 * distancia * math.tan(fovVertical / 2)
    return (Aimbot_OffsetPixels / camera.ViewportSize.Y) * alturaTelaStuds
end

local function EstaVisivel(alvoParte)
    local camera = Workspace.CurrentCamera; local cameraPos = camera.CFrame.Position
    local params = RaycastParams.new(); params.FilterType = Enum.RaycastFilterType.Exclude
    params.FilterDescendantsInstances = {Player.Character}; params.IgnoreWater = true
    local resultado = Workspace:Raycast(cameraPos, (alvoParte.Position - cameraPos), params)
    if not resultado then return true end
    local alvo = alvoParte.Parent
    if alvo and resultado.Instance:IsDescendantOf(alvo) then return true end
    return false
end

local function SaoDoMesmoTime(jogadorA, jogadorB)
    local teamA = jogadorA.Team; local teamB = jogadorB.Team
    if teamA and teamB and teamA == teamB then return true end
    if teamA and teamB and teamA.TeamColor and teamB.TeamColor then
        if teamA.TeamColor == teamB.TeamColor then return true end
    end
    return false
end

local ArmasPermitidasAIM = {
    ["M4A1"] = true,
    ["DEAGLE"] = true,
    ["Remington 870"] = true,
    ["MP40"] = true,
    ["AK-47"] = true,
    ["MP5"] = true,
    ["Sawed-Off"] = true,
    ["P90"] = true,
    ["Revolver"] = true,
    ["G36C"] = true,
    ["BERETTA"] = true,
    ["HK416"] = true,
}

local function TemItemEquipado()
    if not Player.Character then return false end
    for _, child in pairs(Player.Character:GetChildren()) do
        if child:IsA("Tool") and ArmasPermitidasAIM[child.Name] then
            return true
        end
    end
    return false
end

local function ObterJogadorMaisProximoDaMira()
    if Aimbot_SoComItem and not TemItemEquipado() then return nil end
    local melhorAlvo = nil; local melhorPontuacao = math.huge
    local camera = Workspace.CurrentCamera; local viewportTamanho = camera.ViewportSize
    local pontoMiraTela = Vector2.new(viewportTamanho.X / 2, (viewportTamanho.Y / 2) + Aimbot_OffsetTelaY)
    local cameraPos = camera.CFrame.Position
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player and p.Character and p.Character:FindFirstChildOfClass("Humanoid") and p.Character:FindFirstChildOfClass("Humanoid").Health > 0 then
            if JogadoresIgnorados[p.Name] then continue end
            if Aimbot_TeamCheck and SaoDoMesmoTime(Player, p) then continue end
            local parteBuscada = nil
            if Aimbot_ParteAlvo == "Head" then parteBuscada = p.Character:FindFirstChild("Head")
            elseif Aimbot_ParteAlvo == "Torso" then parteBuscada = p.Character:FindFirstChild("UpperTorso") or p.Character:FindFirstChild("Torso") or p.Character:FindFirstChild("HumanoidRootPart")
            elseif Aimbot_ParteAlvo == "Perna" then parteBuscada = p.Character:FindFirstChild("RightLowerLeg") or p.Character:FindFirstChild("LeftLowerLeg") or p.Character:FindFirstChild("RightFoot") or p.Character:FindFirstChild("LeftFoot") or p.Character:FindFirstChild("HumanoidRootPart") end
            if parteBuscada then
                if Aimbot_WallCheck and not EstaVisivel(parteBuscada) then continue end
                local offsetDinamico = CalcularOffset3D_Dinamico(camera, parteBuscada.Position)
                local posicaoAlvo3D = parteBuscada.Position + Vector3.new(0, -offsetDinamico, 0)
                local posicaoTela, visivel = camera:WorldToViewportPoint(posicaoAlvo3D)
                if visivel then
                    local distanciaReal = (parteBuscada.Position - cameraPos).Magnitude
                    local distanciaDaMira = (Vector2.new(posicaoTela.X, posicaoTela.Y) - pontoMiraTela).Magnitude
                    if not Aimbot_UsarFOV or distanciaDaMira <= Aimbot_RaioFOV then
                        local pontuacao = Aimbot_PrioridadeDistancia and (distanciaReal * 0.7 + distanciaDaMira * 0.3) or distanciaDaMira
                        if pontuacao < melhorPontuacao then melhorPontuacao = pontuacao; melhorAlvo = p end
                    end
                end
            end
        end
    end
    return melhorAlvo
end

local function AtualizarHighlightAlvo(alvo)
    if AimbotAlvoHighlight and (not alvo or AimbotAlvoAtual ~= alvo) then
        pcall(function() AimbotAlvoHighlight:Destroy() end)
        AimbotAlvoHighlight = nil
    end
    AimbotAlvoAtual = alvo
    if not Aimbot_DestacarAlvo or not alvo or not alvo.Character then return end
    if not AimbotAlvoHighlight or AimbotAlvoHighlight.Parent ~= alvo.Character then
        local highlight = Instance.new("Highlight")
        highlight.Name = "NoxysAimbotTarget"
        highlight.Adornee = alvo.Character
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        highlight.FillColor = Color3.fromRGB(255, 220, 70)
        highlight.FillTransparency = 0.72
        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
        highlight.OutlineTransparency = 0
        highlight.Parent = alvo.Character
        AimbotAlvoHighlight = highlight
    end
end

--========================================================--
-- AUTO JJ
--========================================================--

local function CarregarScriptJJ()
    if not AutoJJ_Script then pcall(function() AutoJJ_Script = loadstring(game:HttpGet("https://raw.githubusercontent.com/CoiledTom/EB-do-delta-/refs/heads/main/auto%20jjs%20EB%20do%20delta%20com%20delay.lua")) end) end
end
local function DestruirPainelJJ()
    pcall(function()
        local todosGuis = {}
        for _, gui in pairs(CoreGui:GetChildren()) do if gui:IsA("ScreenGui") then table.insert(todosGuis, gui) end end
        for _, gui in pairs(PlayerGui:GetChildren()) do if gui:IsA("ScreenGui") then table.insert(todosGuis, gui) end end
        for _, gui in pairs(todosGuis) do
            local nome = gui.Name:lower()
            if nome:find("auto") or nome:find("jj") then gui:Destroy()
            else for _, child in pairs(gui:GetDescendants()) do if (child:IsA("TextLabel") or child:IsA("TextButton")) and child.Text == "Auto jjs" then gui:Destroy(); break end end
            end
        end
    end)
    AutoJJ_Script = nil
end
local function IniciarAutoJJ() CarregarScriptJJ(); if AutoJJ_Script then pcall(function() AutoJJ_Script() end) end end

--========================================================--
-- BLOCOS PARKOUR
--========================================================--

local data = {
    {pos = Vector3.new(185.03, 5.04, -661.62), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.39, 13.26, -661.39), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.11, 21.28, -661.68), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.30, 29.29, -661.72), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(186.08, 37.36, -661.76), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(186.08, 45.36, -661.76), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(186.25, 53.36, -661.86), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.82, 61.36, -662.00), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.60, 69.40, -661.98), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.71, 77.42, -661.90), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(185.28, 85.44, -662.14), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.42, 93.48, -662.05), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.08, 5.26, -618.66), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.36, 13.28, -618.65), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(185.32, 21.29, -618.70), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.65, 29.34, -618.67), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(186.12, 37.36, -618.43), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(186.30, 45.34, -617.87), size = Vector3.new(5.00, 0.50, 16.00)},
    {pos = Vector3.new(185.41, 53.36, -618.05), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(185.62, 61.40, -617.79), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.58, 69.40, -617.79), size = Vector3.new(4.00, 0.50, 17.00)},
    {pos = Vector3.new(185.87, 77.42, -618.33), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(185.73, 85.44, -618.62), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(185.68, 93.48, -618.49), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(270.22, 4.24, -661.72), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(277.86, 4.24, -655.56), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(269.80, 4.24, -649.96), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(284.45, 4.24, -660.72), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(284.59, 4.24, -650.23), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(267.14, 5.04, -627.94), size = Vector3.new(4.00, 0.50, 32.00)},
    {pos = Vector3.new(288.33, 5.04, -627.86), size = Vector3.new(4.00, 0.50, 31.00)},
    {pos = Vector3.new(277.48, 7.63, -591.97), size = Vector3.new(4.00, 0.50, 27.00)},
    {pos = Vector3.new(271.76, 7.64, -569.98), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(279.08, 7.64, -567.95), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(265.48, 7.64, -564.93), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(271.34, 7.64, -559.70), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(259.46, 7.64, -561.92), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(260.50, 7.64, -554.72), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(250.93, 7.64, -561.91), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(248.66, 7.64, -554.60), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(243.06, 7.64, -565.74), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(235.96, 7.64, -560.93), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(240.08, 7.64, -592.47), size = Vector3.new(4.00, 0.50, 27.00)},
    {pos = Vector3.new(394.19, 5.06, -850.78), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(389.19, 7.45, -855.82), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(392.91, 5.06, -861.09), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(384.07, 10.15, -855.95), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(377.51, 12.94, -855.73), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(370.87, 14.68, -855.83), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(365.12, 16.20, -856.03), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(358.50, 17.61, -855.96), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(351.76, 19.27, -856.05), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(345.37, 20.53, -855.83), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(339.00, 21.54, -855.95), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(334.51, 21.54, -850.11), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(328.83, 21.54, -855.97), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(335.00, 21.54, -861.70), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(324.43, 21.54, -861.35), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(324.69, 21.54, -850.36), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(318.73, 21.54, -855.70), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(313.47, 21.54, -861.90), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(308.27, 21.54, -855.88), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(314.49, 21.54, -850.04), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(303.58, 21.54, -861.59), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(303.61, 21.54, -850.61), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(298.50, 21.54, -856.16), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(394.87, 4.87, -892.93), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(394.65, 4.87, -902.16), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(389.37, 5.04, -892.40), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(389.57, 5.04, -903.24), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(383.37, 7.79, -892.63), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(383.64, 7.79, -903.27), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(378.64, 9.35, -892.48), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(378.64, 9.35, -903.24), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(373.27, 11.53, -892.90), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(372.82, 11.53, -903.33), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(368.47, 11.53, -892.69), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(367.91, 11.53, -903.02), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(362.88, 11.53, -894.04), size = Vector3.new(4.00, 0.50, 7.00)},
    {pos = Vector3.new(362.03, 11.53, -901.74), size = Vector3.new(4.00, 0.50, 6.00)},
    {pos = Vector3.new(352.36, 11.53, -894.75), size = Vector3.new(4.00, 0.50, 6.00)},
    {pos = Vector3.new(352.47, 11.53, -902.21), size = Vector3.new(4.00, 0.50, 6.00)},
    {pos = Vector3.new(333.21, 11.53, -897.81), size = Vector3.new(5.00, 0.50, 12.00)},
    {pos = Vector3.new(325.73, 11.53, -898.53), size = Vector3.new(5.00, 0.50, 12.00)},
    {pos = Vector3.new(318.25, 11.53, -897.79), size = Vector3.new(5.00, 0.50, 11.00)},
    {pos = Vector3.new(310.71, 11.53, -898.95), size = Vector3.new(5.00, 0.50, 12.00)},
    {pos = Vector3.new(303.10, 11.53, -898.29), size = Vector3.new(5.00, 0.50, 12.00)},
    {pos = Vector3.new(295.54, 11.53, -898.01), size = Vector3.new(5.00, 0.50, 12.00)},
    {pos = Vector3.new(288.21, 11.53, -898.79), size = Vector3.new(5.00, 0.50, 12.00)},
    {pos = Vector3.new(149.54, 1.04, -856.00), size = Vector3.new(3.00, 0.50, 13.00)},
    {pos = Vector3.new(148.21, 7.50, -855.93), size = Vector3.new(4.00, 0.50, 13.00)},
    {pos = Vector3.new(148.27, 13.38, -856.27), size = Vector3.new(4.00, 0.50, 13.00)},
    {pos = Vector3.new(162.71, 13.70, -859.82), size = Vector3.new(8.00, 0.50, 5.00)},
    {pos = Vector3.new(172.33, 13.70, -852.89), size = Vector3.new(8.00, 0.50, 5.00)},
    {pos = Vector3.new(181.41, 13.70, -859.17), size = Vector3.new(8.00, 0.50, 5.00)},
    {pos = Vector3.new(191.97, 13.70, -853.25), size = Vector3.new(8.00, 0.50, 5.00)},
    {pos = Vector3.new(217.92, 13.122, -848.28), size = Vector3.new(32.00, 0.50, 4.00)},
    {pos = Vector3.new(217.99, 13.122, -863.62), size = Vector3.new(29.00, 0.50, 4.00)},
    {pos = Vector3.new(243.52, 13.122, -855.57), size = Vector3.new(14.00, 0.50, 3.00)},
    {pos = Vector3.new(150.43, 5.27, -897.87), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(150.71, 13.28, -898.27), size = Vector3.new(4.00, 0.50, 17.00)},
    {pos = Vector3.new(150.28, 21.29, -898.42), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(150.45, 29.30, -898.11), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(150.52, 37.31, -898.44), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(150.56, 45.32, -897.73), size = Vector3.new(4.00, 0.50, 15.00)},
    {pos = Vector3.new(150.95, 53.33, -898.02), size = Vector3.new(4.00, 0.50, 16.00)},
    {pos = Vector3.new(167.23, 53.26, -903.84), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(172.90, 53.26, -897.50), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(167.09, 53.26, -891.043), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(181.65, 53.26, -902.043), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(180.48, 53.26, -893.06), size = Vector3.new(5.00, 0.50, 5.00)},
    {pos = Vector3.new(201.203, 53.798, -896.443), size = Vector3.new(7.00, 0.50, 7.00)},
    {pos = Vector3.new(211.682, 53.798, -896.443), size = Vector3.new(7.00, 0.50, 7.00)},
    {pos = Vector3.new(222.218, 53.798, -896.443), size = Vector3.new(7.00, 0.50, 7.00)},
    {pos = Vector3.new(232.595, 53.798, -896.443), size = Vector3.new(7.00, 0.50, 7.00)},
}

local function CreateHitboxes()
    for _, v in ipairs(data) do
        local p = Instance.new("Part", workspace)
        p.Name = "BlocoParkour"; p.Size = v.size; p.Position = v.pos
        p.Anchored = true; p.CanCollide = true; p.Transparency = 0.7
        p.Color = Color3.fromRGB(0, 255, 100); p.Material = Enum.Material.Neon
        table.insert(Hitboxes, p)
    end
end
local function RemoveHitboxes() for _, v in pairs(Hitboxes) do if v then v:Destroy() end end; Hitboxes = {} end

--========================================================--
-- LOOP PRINCIPAL
--========================================================--

RunService.RenderStepped:Connect(function()
    local camera = Workspace.CurrentCamera; local viewportTamanho = camera.ViewportSize
    AtualizarCaixasESP2D(camera)
    pcall(function() AtualizarSkeleton(camera) end)
    if Aimbot_UsarFOV and FOVCircle then FOVCircle.Position = Vector2.new(viewportTamanho.X / 2, (viewportTamanho.Y / 2) + Aimbot_OffsetTelaY); FOVCircle.Radius = Aimbot_RaioFOV; FOVCircle.Thickness = Aimbot_EspessuraFOV end
    local alvoAimbot = nil
    if Aimbot_Ativado then
        alvoAimbot = ObterJogadorMaisProximoDaMira()
        AtualizarHighlightAlvo(alvoAimbot)
        if alvoAimbot and alvoAimbot.Character then
            local alvo = alvoAimbot
            local parteRastrear = nil
            if Aimbot_ParteAlvo == "Head" then parteRastrear = alvo.Character:FindFirstChild("Head")
            elseif Aimbot_ParteAlvo == "Torso" then parteRastrear = alvo.Character:FindFirstChild("UpperTorso") or alvo.Character:FindFirstChild("Torso") or alvo.Character:FindFirstChild("HumanoidRootPart")
            elseif Aimbot_ParteAlvo == "Perna" then parteRastrear = alvo.Character:FindFirstChild("RightLowerLeg") or alvo.Character:FindFirstChild("LeftLowerLeg") or alvo.Character:FindFirstChild("RightFoot") or alvo.Character:FindFirstChild("LeftFoot") or alvo.Character:FindFirstChild("HumanoidRootPart") end
            if parteRastrear then local offsetDinamico = CalcularOffset3D_Dinamico(camera, parteRastrear.Position); local alvoPos = parteRastrear.Position + Vector3.new(0, -offsetDinamico, 0); camera.CFrame = camera.CFrame:Lerp(CFrame.new(camera.CFrame.Position, alvoPos), Aimbot_Suavidade) end
        end
    else
        AtualizarHighlightAlvo(nil)
    end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player and p.Character then
            local root = p.Character:FindFirstChild("HumanoidRootPart"); local hum = p.Character:FindFirstChildOfClass("Humanoid")
            if hum and root and hum.Health > 0 and Hitbox_Ativado then root.Size = Vector3.new(Hitbox_Tamanho, Hitbox_Tamanho, Hitbox_Tamanho); root.Transparency = Hitbox_Transparencia; pcall(function() root.Material = Hitbox_Material end); pcall(function() root.Color = Hitbox_Cor end); root.CanCollide = false end
        end
    end
    local centroBaseTela = Vector2.new(viewportTamanho.X / 2, viewportTamanho.Y)
    for p, tracer in pairs(LinhasTracers) do
        if ESP_GlobalAtivado and ESP_Tracers and p ~= Player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
            local root = p.Character.HumanoidRootPart; local hum = p.Character:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                local mostrar = true
                if ESP_TimesAtivado then local timePlayer = GetPlayerTime(p); if timePlayer then mostrar = TimesMarcados[timePlayer] or false else mostrar = false end end
                if mostrar then local posTela, visivel = camera:WorldToViewportPoint(root.Position)
                    if visivel then tracer.From = centroBaseTela; tracer.To = Vector2.new(posTela.X, posTela.Y); tracer.Visible = true else tracer.Visible = false end
                else tracer.Visible = false end
            else tracer.Visible = false end
        else if tracer then tracer.Visible = false end end
    end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= Player and p.Character then
            local head = p.Character:FindFirstChild("Head")
            local hum = p.Character:FindFirstChildOfClass("Humanoid")
            if head and hum then
                if ESP_GlobalAtivado and ESP_Names then
                    local nameGui = head:FindFirstChild("NoahzinName")
                    if nameGui and nameGui:FindFirstChild("TextLabel") then
                        nameGui.TextLabel.Text = p.Name
                    end
                end
                if ESP_TimesNomes then
                    local timeGui = head:FindFirstChild("NoahzinTime")
                    if timeGui and timeGui:FindFirstChild("TextLabel") then
                        timeGui.TextLabel.Text = p.Team and p.Team.Name or "SEM TIME"
                    end
                end
                if ESP_GlobalAtivado and ESP_Vida then
                    local vidaGui = head:FindFirstChild("NoahzinVida")
                    if vidaGui and vidaGui:FindFirstChild("TextLabel") then
                        vidaGui.TextLabel.Text = string.format("[%d/%d]", math.floor(hum.Health), math.floor(hum.MaxHealth))
                    end
                end
                if ESP_GlobalAtivado and ESP_Distancia then
                    local distanciaGui = head:FindFirstChild("NoahzinDistancia")
                    local meuHead = Player.Character and Player.Character:FindFirstChild("Head")
                    if distanciaGui and distanciaGui:FindFirstChild("TextLabel") and meuHead then
                        distanciaGui.TextLabel.Text = string.format("[%dm]", math.floor((head.Position - meuHead.Position).Magnitude))
                    end
                end
            end
        end
    end
    if BlocosAtivado and Player.Character then
        local hrp = Player.Character:FindFirstChild("HumanoidRootPart")
        if hrp then local velY = hrp.AssemblyLinearVelocity.Y; for _, part in pairs(Hitboxes) do if part then part.CanCollide = (velY <= 0.5 and hrp.Position.Y > (part.Position.Y + part.Size.Y / 2 - 1)) end end end
    end
end)

--========================================================--
-- BOTÃO DASH
--========================================================--

local function CriarBotaoDash()
   dashButtonScreen = Instance.new("ScreenGui"); dashButtonScreen.Name = "DashButtonScreen"; dashButtonScreen.Parent = CoreGui
   local mainButton = Instance.new("TextButton"); mainButton.Size = UDim2.new(0, 50, 0, 50); mainButton.Position = UDim2.new(0.87, 0, 0.5, 0)
   mainButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35); mainButton.Text = "DASH"; mainButton.TextColor3 = Color3.fromRGB(255, 255, 255)
   mainButton.Font = Enum.Font.SourceSansBold; mainButton.TextSize = 12; mainButton.BorderSizePixel = 0; mainButton.Active = true; mainButton.Draggable = true; mainButton.Parent = dashButtonScreen
   Instance.new("UICorner", mainButton).CornerRadius = UDim.new(0, 10)
   local uiStroke = Instance.new("UIStroke", mainButton); uiStroke.Color = Color3.fromRGB(0, 120, 255); uiStroke.Thickness = 2
   mainButton.MouseButton1Click:Connect(function() local char = Player.Character; if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = char.HumanoidRootPart.CFrame * CFrame.new(0, 0, -dashStuds) end end)
end
local function RemoverBotaoDash() if CoreGui:FindFirstChild("DashButtonScreen") then CoreGui.DashButtonScreen:Destroy() end end

--========================================================--
-- TROCA DE SERVIDOR
--========================================================--

local function TrocarServidor(tipo)
    local placeId = game.PlaceId
    if tipo == "cheio" then pcall(function() TeleportService:Teleport(placeId) end)
    elseif tipo == "vazio" then pcall(function() TeleportService:TeleportToPlaceInstance(placeId, "", Player) end)
    elseif tipo == "rejoin" then pcall(function() TeleportService:Teleport(placeId) end)
    end
end

--========================================================--
-- FAKE ADMIN
--========================================================--

local function EncontrarJogadorAdmin(nome)
    nome = nome:lower()
    for _, p in pairs(Players:GetPlayers()) do
        if p.Name:lower():find(nome, 1, true) or (p.DisplayName and p.DisplayName:lower():find(nome, 1, true)) then return p end
    end
    return nil
end

local function ExecutarComandoAdmin(mensagem)
    if not Admin_Ativado then return end
    if not mensagem:sub(1, #Admin_Prefixo) == Admin_Prefixo then return end
    
    local comando = mensagem:sub(#Admin_Prefixo + 1)
    local args = comando:split(" ")
    local cmd = args[1]:lower()
    table.remove(args, 1)
    local resto = table.concat(args, " ")
    
    if cmd == "to" or cmd == "tp" then
        local alvo = EncontrarJogadorAdmin(resto)
        if alvo and alvo.Character and alvo.Character:FindFirstChild("HumanoidRootPart") and Player.Character then
            Player.Character.HumanoidRootPart.CFrame = alvo.Character.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
        end
    elseif cmd == "bring" then
        local alvo = EncontrarJogadorAdmin(resto)
        if alvo and alvo.Character and alvo.Character:FindFirstChild("HumanoidRootPart") and Player.Character then
            alvo.Character.HumanoidRootPart.CFrame = Player.Character.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
        end
    elseif cmd == "respawn" then
        if resto == "all" then for _, p in pairs(Players:GetPlayers()) do if p ~= Player then pcall(function() p:LoadCharacter() end) end end
        else local alvo = EncontrarJogadorAdmin(resto); if alvo then pcall(function() alvo:LoadCharacter() end) end
        end
    elseif cmd == "invis" then
        if Player.Character then for _, part in pairs(Player.Character:GetDescendants()) do if part:IsA("BasePart") then part.Transparency = 0.9 end end; Admin_Invisivel = true end
    elseif cmd == "vis" then
        if Player.Character then for _, part in pairs(Player.Character:GetDescendants()) do if part:IsA("BasePart") then part.Transparency = 0 end end; Admin_Invisivel = false end
    elseif cmd == "view" then
        local alvo = EncontrarJogadorAdmin(resto)
        if alvo and alvo.Character then Workspace.CurrentCamera.CameraSubject = alvo.Character:FindFirstChild("Humanoid") or alvo.Character end
    elseif cmd == "unview" then
        Workspace.CurrentCamera.CameraSubject = Player.Character and Player.Character:FindFirstChild("Humanoid") or Player.Character
    elseif cmd == "sit" then
        local alvo = EncontrarJogadorAdmin(resto)
        if alvo and alvo.Character and alvo.Character:FindFirstChild("Humanoid") then alvo.Character.Humanoid.Sit = true end
    elseif cmd == "jump" then
        local alvo = EncontrarJogadorAdmin(resto)
        if alvo and alvo.Character and alvo.Character:FindFirstChild("Humanoid") then alvo.Character.Humanoid.Jump = true end
    elseif cmd == "hipheight" then
        local valor = tonumber(resto)
        if valor and Player.Character and Player.Character:FindFirstChild("Humanoid") then Player.Character.Humanoid.HipHeight = valor end
    elseif cmd == "char" or cmd == "skin" then
        local alvo = EncontrarJogadorAdmin(resto)
        if alvo and alvo.Character and Player.Character and Player.Character:FindFirstChild("Humanoid") then
            local alvoHum = alvo.Character:FindFirstChildOfClass("Humanoid")
            if alvoHum then
                local desc = alvoHum:GetAppliedDescription()
                Player.Character.Humanoid:ApplyDescription(desc)
            end
        end
    end
end

task.spawn(function()
    pcall(function() game:GetService("TextChatService").MessageReceived:Connect(function(message) if message.TextSource == Player then ExecutarComandoAdmin(message.Text) end end) end)
    pcall(function() Player.Chatted:Connect(function(mensagem) ExecutarComandoAdmin(mensagem) end) end)
end)

--========================================================--
-- CHATWINDOW
--========================================================--

local function AtivarChatWindow()
    if ChatWindow_Conexoes then DesativarChatWindow() end
    local StarterGui = game:GetService("StarterGui"); local TextChatService = game:GetService("TextChatService")
    ChatWindow_Conexoes = {}; ChatWindow_Conexoes.ChatWindow = {}
    local toggleEvent = Instance.new("BindableEvent"); toggleEvent.Name = "ToggleVisibility"; ChatWindow_Conexoes.ChatWindow.ToggleVisibility = toggleEvent
    local setVisibleEvent = Instance.new("BindableEvent"); setVisibleEvent.Name = "SetVisible"; ChatWindow_Conexoes.ChatWindow.SetVisible = setVisibleEvent
    local visibilityChangedEvent = Instance.new("BindableEvent"); visibilityChangedEvent.Name = "VisibilityStateChanged"; ChatWindow_Conexoes.ChatWindow.VisibilityStateChanged = visibilityChangedEvent
    pcall(function() StarterGui:SetCore("CoreGuiChatConnections", ChatWindow_Conexoes) end)
    local function ToggleChat() local wc = TextChatService:FindFirstChildOfClass("ChatWindowConfiguration"); if wc then wc.Enabled = not wc.Enabled; pcall(function() visibilityChangedEvent:Fire(wc.Enabled) end) end end
    toggleEvent.Event:Connect(function() ToggleChat() end)
    setVisibleEvent.Event:Connect(function(visible) local wc = TextChatService:FindFirstChildOfClass("ChatWindowConfiguration"); if wc then wc.Enabled = visible; pcall(function() visibilityChangedEvent:Fire(visible) end) end end)
end

local function DesativarChatWindow()
    if ChatWindow_Conexoes then pcall(function() ChatWindow_Conexoes.ChatWindow.ToggleVisibility:Destroy(); ChatWindow_Conexoes.ChatWindow.SetVisible:Destroy(); ChatWindow_Conexoes.ChatWindow.VisibilityStateChanged:Destroy() end); ChatWindow_Conexoes = nil end
    pcall(function()
        local StarterGui = game:GetService("StarterGui"); local TextChatService = game:GetService("TextChatService")
        local wc = TextChatService:FindFirstChildOfClass("ChatWindowConfiguration"); if wc then wc.Enabled = false end; task.wait(0.1)
        local novas = {}; novas.ChatWindow = {}
        local te = Instance.new("BindableEvent"); te.Name = "ToggleVisibility"; novas.ChatWindow.ToggleVisibility = te
        local se = Instance.new("BindableEvent"); se.Name = "SetVisible"; novas.ChatWindow.SetVisible = se
        local vc = Instance.new("BindableEvent"); vc.Name = "VisibilityStateChanged"; novas.ChatWindow.VisibilityStateChanged = vc
        te.Event:Connect(function() local w = TextChatService:FindFirstChildOfClass("ChatWindowConfiguration"); if w then w.Enabled = not w.Enabled; vc:Fire(w.Enabled) end end)
        se.Event:Connect(function(v) local w = TextChatService:FindFirstChildOfClass("ChatWindowConfiguration"); if w then w.Enabled = v; vc:Fire(v) end end)
        StarterGui:SetCore("CoreGuiChatConnections", novas)
    end)
end

--========================================================--
-- MAIN PAGE (PRINCIPAL)
--========================================================--

local MainPage = Pages.Main

local AutoJJSection = Section(MainPage, "AUTO JJ")
Toggle(AutoJJSection, "AUTO JJ", false, function(State)
    AutoJJ_Ativado = State
    if State then IniciarAutoJJ() else DestruirPainelJJ() end
end)

local BlocosSection = Section(MainPage, "BLOCOS")
Toggle(BlocosSection, "BLOCOS PARKOUR", false, function(State)
    BlocosAtivado = State
    if State then CreateHitboxes() else RemoveHitboxes() end
end)

--========================================================--
-- COMBAT PAGE
--========================================================--

local CombatPage = Pages.Combat

local AimbotSection = Section(CombatPage, "AIMBOT")
Toggle(AimbotSection, "AIMBOT", false, function(State) Aimbot_Ativado = State end)
Toggle(AimbotSection, "SHOW FOV", false, function(State) Aimbot_UsarFOV = State; FOVCircle.Visible = State end)
Toggle(AimbotSection, "TEAM CHECK", false, function(State) Aimbot_TeamCheck = State end)
Toggle(AimbotSection, "WALL CHECK", false, function(State) Aimbot_WallCheck = State end)
Toggle(AimbotSection, "PRIORIDADE DISTÂNCIA", false, function(State) Aimbot_PrioridadeDistancia = State end)
Toggle(AimbotSection, "DESTACAR ALVO", false, function(State) Aimbot_DestacarAlvo = State; AtualizarHighlightAlvo(State and AimbotAlvoAtual or nil) end)
Toggle(AimbotSection, "AIM GUN", false, function(State) Aimbot_SoComItem = State end)
Slider(AimbotSection, "FOV RADIUS", 20, 300, 100, function(Value) Aimbot_RaioFOV = Value; FOVCircle.Radius = Value end)
Slider(AimbotSection, "SMOOTHNESS", 1, 70, 15, function(Value) Aimbot_Suavidade = Value / 100 end)
Slider(AimbotSection, "FOV THICKNESS", 1, 10, 2, function(Value)
    Aimbot_EspessuraFOV = Value
    FOVCircle.Thickness = Value
end)

local BodyTargetSection = Section(CombatPage, "CORPO ALVO")
Button(BodyTargetSection, "HEAD", function()
    Aimbot_ParteAlvo = "Head"
    Notify("Alvo: Cabeça")
end)
Button(BodyTargetSection, "TORSO", function()
    Aimbot_ParteAlvo = "Torso"
    Notify("Alvo: Torso")
end)
Button(BodyTargetSection, "PERNA", function()
    Aimbot_ParteAlvo = "Perna"
    Notify("Alvo: Perna")
end)

local FOVColorSection = Section(CombatPage, "COR DO FOV")
ColorPicker(FOVColorSection, "FOV", Aimbot_CorFOV)

local HitboxSection = Section(CombatPage, "HITBOX")
Toggle(HitboxSection, "HITBOX", false, function(State)
    Hitbox_Ativado = State
    if not State then for _, p in pairs(Players:GetPlayers()) do if p ~= Player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then p.Character.HumanoidRootPart.Size = Vector3.new(2, 2, 1); p.Character.HumanoidRootPart.Transparency = 1 end end end
end)
Slider(HitboxSection, "HITBOX SIZE", 5, 50, 20, function(Value) Hitbox_Tamanho = Value end)
Slider(HitboxSection, "HITBOX TRANSP", 0, 100, 70, function(Value)
    Hitbox_Transparencia = Value / 100
end)

local HitboxColorSection = Section(CombatPage, "COR DA HITBOX")
ColorPicker(HitboxColorSection, "Hitbox", Hitbox_Cor)

--========================================================--
-- VISUAL PAGE
--========================================================--

local VisualPage = Pages.Visual

local ESPSection = Section(VisualPage, "ESP")
Toggle(ESPSection, "ATIVAR ESP", false, function(State) ESP_GlobalAtivado = State; AtualizarESP() end)
Toggle(ESPSection, "CORES AUTOMÁTICAS POR TIME", false, function(State) ESP_CoresPorTime = State; AtualizarESP() end)
Toggle(ESPSection, "ESP COMPLETO", false, function(State) ESP_Chams=State; ESP_Box=State; ESP_Names=State; ESP_Tracers=State; ESP_Skeleton=State; AtualizarESP() end)
Toggle(ESPSection, "ESP BOX", false, function(State) ESP_Box=State; AtualizarESP() end)
Slider(ESPSection, "ESP BOX THICKNESS", 1, 10, 2, function(Value)
    ESP_BoxThickness = Value
end)
Toggle(ESPSection, "ESP NAMES", false, function(State) ESP_Names=State; AtualizarESP() end)
Toggle(ESPSection, "ESP CHAMS", false, function(State) ESP_Chams=State; AtualizarESP() end)
Toggle(ESPSection, "ESP TRACERS", false, function(State) ESP_Tracers=State; AtualizarESP() end)
Toggle(ESPSection, "ESP SKELETON", false, function(State)
    ESP_Skeleton = State
    local camera = Workspace.CurrentCamera
    if camera then pcall(function() AtualizarSkeleton(camera) end) end
end)
Toggle(ESPSection, "ESP VIDA", false, function(State) ESP_Vida=State; AtualizarESP() end)
Toggle(ESPSection, "ESP TIMES", false, function(State) ESP_TimesNomes=State; AtualizarESP() end)
Toggle(ESPSection, "ESP DISTÂNCIA", false, function(State) ESP_Distancia=State; AtualizarESP() end)

local TimesSection = Section(VisualPage, "ESP POR TIMES")
Toggle(TimesSection, "FILTRAR POR TIMES", false, function(State)
    ESP_TimesAtivado = State
    AtualizarESP()
end)
local timesNomes = {"[CI]", "[EB]", "[OF]", "[BAC]", "[BPE]", "[BFE]", "[CIE]", "[BIP]", "[CAAT]", "[CIGS]", "[MD]", "[REC MEC]"}
for _, time in ipairs(timesNomes) do
    Toggle(TimesSection, time, TimesMarcados[time], function(State)
        TimesMarcados[time] = State
        AtualizarESP()
    end)
end

local ESPColorSection = Section(VisualPage, "COR DO ESP")
ColorPicker(ESPColorSection, "ESP", CorESP)

local CameraSection = Section(VisualPage, "CAMERA")
Slider(CameraSection, "FOV", 40, 120, Config.FOV, function(Value)
    Config.FOV = Value
    if Config.FOVEnabled then
        local Camera = Workspace.CurrentCamera
        if Camera then Camera.FieldOfView = Value end
    end
end)
Toggle(CameraSection, "FOV ENABLED", Config.FOVEnabled, function(State)
    Config.FOVEnabled = State
    local Camera = Workspace.CurrentCamera
    if Camera then Camera.FieldOfView = State and Config.FOV or 70 end
end)
Button(CameraSection, "FULLBRIGHT", function()
    Lighting.Brightness = 2
    Lighting.ClockTime = 14
    Lighting.GlobalShadows = false
    Lighting.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
    Notify("Fullbright ON")
end)

--========================================================--
-- MOVEMENT PAGE
--========================================================--

local MovementPage = Pages.Movement

local SpeedSection = Section(MovementPage, "SPEED HACK")
Toggle(SpeedSection, "SPEED HACK", false, function(State)
    SpeedHack_Ativado = State
    if State then
        if SpeedHack_Conexao then SpeedHack_Conexao:Disconnect() end
        SpeedHack_Conexao = RunService.RenderStepped:Connect(function()
            if not SpeedHack_Ativado then return end
            local char = Player.Character
            if not char then return end
            local root = char:FindFirstChild("HumanoidRootPart")
            local humanoid = char:FindFirstChild("Humanoid")
            if not root or not humanoid then return end
            if humanoid.MoveDirection.Magnitude > 0 then
                local moveDirection = humanoid.MoveDirection
                local speed = SpeedHack_Velocidade * 0.5
                root.CFrame = root.CFrame + (moveDirection * speed / 60)
            end
        end)
    else
        if SpeedHack_Conexao then SpeedHack_Conexao:Disconnect(); SpeedHack_Conexao = nil end
    end
end)
Slider(SpeedSection, "SPEED VALUE", 1, 500, 50, function(Value) SpeedHack_Velocidade = Value end)

local DashSection = Section(MovementPage, "DASH")
Toggle(DashSection, "DASH BUTTON", false, function(State)
    if State then CriarBotaoDash() else RemoverBotaoDash() end
end)
Slider(DashSection, "DASH DIST", 10, 100, 30, function(Value) dashStuds = Value end)

local SpecialSection = Section(MovementPage, "SPECIAL")
Toggle(SpecialSection, "UNDERGROUND", false, function(State)
    Underground_Ativado = State
    local char = Player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then Underground_Ativado = false; return end
    local root = char.HumanoidRootPart
    if State then
        Underground_Voltando = false
        
        if not UndergroundFloor then
            UndergroundFloor = Instance.new("Part")
            UndergroundFloor.Name = "UndergroundFloor"
            UndergroundFloor.Size = Vector3.new(10, 1, 10)
            UndergroundFloor.Anchored = true
            UndergroundFloor.CanCollide = true
            UndergroundFloor.Transparency = 1
            UndergroundFloor.Parent = Workspace
        end
        UndergroundFloor.Position = Vector3.new(root.Position.X, Underground_yAbaixo - 3, root.Position.Z)
        
        if not Underground_CameraPart then
            Underground_CameraPart = Instance.new("Part")
            Underground_CameraPart.Name = "CameraGhost"
            Underground_CameraPart.Size = Vector3.new(1, 1, 1)
            Underground_CameraPart.Anchored = true
            Underground_CameraPart.CanCollide = false
            Underground_CameraPart.Transparency = 1
            Underground_CameraPart.Parent = Workspace
        end
        Underground_CameraPart.CFrame = CFrame.new(root.Position.X, Underground_yCamera, root.Position.Z)
        root.CFrame = CFrame.new(root.Position.X, Underground_yAbaixo, root.Position.Z)
        
        if Underground_Conexao then Underground_Conexao:Disconnect() end
        Underground_Conexao = RunService.Heartbeat:Connect(function()
            if not Underground_Ativado or Underground_Voltando then return end
            local charAtual = Player.Character
            if not charAtual or not charAtual:FindFirstChild("HumanoidRootPart") then return end
            local rootAtual = charAtual.HumanoidRootPart
            if rootAtual.Position.Y < Underground_yAbaixo - 2 then
                rootAtual.CFrame = CFrame.new(rootAtual.Position.X, Underground_yAbaixo, rootAtual.Position.Z)
            end
            if UndergroundFloor then
                UndergroundFloor.Position = Vector3.new(rootAtual.Position.X, Underground_yAbaixo - 3, rootAtual.Position.Z)
            end
            if Underground_CameraPart then
                Underground_CameraPart.CFrame = CFrame.new(rootAtual.Position.X, Underground_yCamera, rootAtual.Position.Z)
            end
        end)
        local camera = Workspace.CurrentCamera
        camera.CameraSubject = Underground_CameraPart
        camera.CameraType = Enum.CameraType.Follow
    else
        Underground_Voltando = true
        if Underground_Conexao then Underground_Conexao:Disconnect(); Underground_Conexao = nil end
        local camera = Workspace.CurrentCamera
        if camera then camera.CameraSubject = Player.Character and Player.Character:FindFirstChild("Humanoid"); camera.CameraType = Enum.CameraType.Follow end
        if Underground_CameraPart then Underground_CameraPart:Destroy(); Underground_CameraPart = nil end
        if UndergroundFloor then UndergroundFloor:Destroy(); UndergroundFloor = nil end
        root.CFrame = CFrame.new(root.Position.X, Underground_yCamera, root.Position.Z)
        task.wait(0.5)
        Underground_Voltando = false
    end
end)
Toggle(SpecialSection, "PASSE LIVRE", false, function(State)
    PasseLivre_Ativado = State
    if State then
        if PasseLivre_Conexao then PasseLivre_Conexao:Disconnect() end
        PasseLivre_Conexao = RunService.Heartbeat:Connect(function()
            if not PasseLivre_Ativado then return end
            local char = Player.Character
            if not char or not char:FindFirstChild("HumanoidRootPart") then return end
            local root = char.HumanoidRootPart
            local posHorizontal = Vector3.new(root.Position.X, 0, root.Position.Z)
            local centroHorizontal = Vector3.new(PasseLivre_Centro.X, 0, PasseLivre_Centro.Z)
            local distancia = (posHorizontal - centroHorizontal).Magnitude
            if distancia <= PasseLivre_Raio then
                if not PasseLivre_NoClipAtivo then
                    PasseLivre_NoClipAtivo = true
                    for _, part in pairs(char:GetDescendants()) do if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then part.CanCollide = false end end
                    root.CanCollide = false
                    Notify("Noclip ON")
                end
            else
                if PasseLivre_NoClipAtivo then
                    PasseLivre_NoClipAtivo = false
                    for _, part in pairs(char:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = true end end
                    Notify("Noclip OFF")
                end
            end
        end)
    else
        if PasseLivre_Conexao then PasseLivre_Conexao:Disconnect(); PasseLivre_Conexao = nil end
        if PasseLivre_NoClipAtivo then
            PasseLivre_NoClipAtivo = false
            local char = Player.Character
            if char then for _, part in pairs(char:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = true end end end
        end
    end
end)

--========================================================--
-- CONFIG PAGE
--========================================================--

local ConfigPage = Pages.Config

local SystemSection = Section(ConfigPage, "SYSTEM")
Toggle(SystemSection, "NOTIFICATIONS", Config.Notifications, function(State) Config.Notifications = State end)

local AdminSection = Section(ConfigPage, "ADMIN")
Toggle(AdminSection, "ADMIN MODE", false, function(State)
    Admin_Ativado = State
    Notify(State and "Admin ON" or "Admin OFF")
end)
Input(AdminSection, "PREFIXO", Admin_Prefixo, function(text)
    if text ~= "" then
        Admin_Prefixo = text
        Notify("Prefixo: " .. text)
    end
end)
Button(AdminSection, "LISTA DE COMANDOS", function()
    Notify("!to !bring !respawn")
    task.wait(1.5)
    Notify("!invis !vis !view !unview")
    task.wait(1.5)
    Notify("!sit !jump !hipheight")
    task.wait(1.5)
    Notify("!char !skin !cmdbar")
end)

local ServerSection = Section(ConfigPage, "SERVER")
Button(ServerSection, "REJOIN", function() task.wait(3); TrocarServidor("rejoin") end)
Button(ServerSection, "SERVER CHEIO", function() task.wait(1); TrocarServidor("cheio") end)
Button(ServerSection, "SERVER VAZIO", function() task.wait(1); TrocarServidor("vazio") end)

local ThemeSection = Section(ConfigPage, "THEME")
Button(ThemeSection, "OLIVE THEME", function()
    Accent = COLORS.Olive
    TopLine.BackgroundColor3 = Accent
    BadgeStroke.Color = Accent
    for _, Page in pairs(Pages) do Page.ScrollBarImageColor3 = Accent end
    Notify("Olive theme")
end)
Button(ThemeSection, "GREEN THEME", function()
    Accent = COLORS.GreenDark
    TopLine.BackgroundColor3 = Accent
    BadgeStroke.Color = Accent
    for _, Page in pairs(Pages) do Page.ScrollBarImageColor3 = Accent end
    Notify("Green theme")
end)

local ActionsSection = Section(ConfigPage, "ACTIONS")
Button(ActionsSection, "DESTRUIR", function()
    AutoJJ_Ativado = false; BlocosAtivado = false; SpeedHack_Ativado = false
    if SpeedHack_Conexao then SpeedHack_Conexao:Disconnect(); SpeedHack_Conexao = nil end
    Underground_Ativado = false; if Underground_Conexao then Underground_Conexao:Disconnect(); Underground_Conexao = nil end
    if Underground_CameraPart then Underground_CameraPart:Destroy(); Underground_CameraPart = nil end
    if UndergroundFloor then UndergroundFloor:Destroy(); UndergroundFloor = nil end
    PasseLivre_Ativado = false; if PasseLivre_Conexao then PasseLivre_Conexao:Disconnect(); PasseLivre_Conexao = nil end
    Admin_Ativado = false; ChatWindow_Ativado = false; DesativarChatWindow()
    RemoveHitboxes(); DestruirPainelJJ(); RemoverBotaoDash()
    if FOVCircle then FOVCircle:Destroy() end
    AtualizarHighlightAlvo(nil)
    for _, p in pairs(Players:GetPlayers()) do if p ~= Player and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then p.Character.HumanoidRootPart.Size = Vector3.new(2, 2, 1); p.Character.HumanoidRootPart.Transparency = 1 end end
    for _, t in pairs(LinhasTracers) do if t then t:Destroy() end end
    for p in pairs(CaixasESP2D) do RemoverCaixaESP2D(p) end
    for p in pairs(EsqueletosESP) do RemoverSkeleton(p) end
    for _, plr in pairs(Players:GetPlayers()) do if plr.Character then local e = plr.Character:FindFirstChild("NoahzinESP") if e then e:Destroy() end; local b = plr.Character:FindFirstChild("NoahzinBox") if b then b:Destroy() end; local h = plr.Character:FindFirstChild("Head") if h and h:FindFirstChild("NoahzinName") then h.NoahzinName:Destroy() end; local d = plr.Character:FindFirstChild("Head") if d and d:FindFirstChild("NoahzinDistancia") then d.NoahzinDistancia:Destroy() end; local tm = plr.Character:FindFirstChild("Head") if tm and tm:FindFirstChild("NoahzinTime") then tm.NoahzinTime:Destroy() end; local v = plr.Character:FindFirstChild("Head") if v and v:FindFirstChild("NoahzinVida") then v.NoahzinVida:Destroy() end end end
    Gui:Destroy()
end)

--========================================================--
-- ANIMAÇÕES DOS BOTÕES
--========================================================--

local function AnimarBotao(button)
    if not button:IsA("TextButton") then return end
    local escala = Instance.new("UIScale")
    escala.Scale = 1
    escala.Parent = button
    local corOriginal = button.BackgroundColor3
    local conectado = false

    button.MouseEnter:Connect(function()
        conectado = true
        TweenService:Create(escala, TweenInfo.new(0.12, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Scale = 1.035}):Play()
        TweenService:Create(button, TweenInfo.new(0.12, Enum.EasingStyle.Quad), {BackgroundColor3 = button.BackgroundColor3:Lerp(Color3.new(1, 1, 1), 0.08)}):Play()
    end)
    button.MouseLeave:Connect(function()
        conectado = false
        TweenService:Create(escala, TweenInfo.new(0.16, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Scale = 1}):Play()
        TweenService:Create(button, TweenInfo.new(0.16, Enum.EasingStyle.Quad), {BackgroundColor3 = corOriginal}):Play()
    end)
    button.MouseButton1Down:Connect(function()
        TweenService:Create(escala, TweenInfo.new(0.06, Enum.EasingStyle.Quad), {Scale = 0.96}):Play()
    end)
    button.MouseButton1Up:Connect(function()
        TweenService:Create(escala, TweenInfo.new(0.1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = conectado and 1.035 or 1}):Play()
    end)
end

for _, object in ipairs(Gui:GetDescendants()) do
    AnimarBotao(object)
end

--========================================================--
-- DRAG DA UI
--========================================================--

local Dragging = false
local DragStart
local StartPosition

Top.InputBegan:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Dragging = true
        DragStart = Input.Position
        StartPosition = Main.Position
    end
end)

UserInputService.InputChanged:Connect(function(Input)
    if not Dragging then return end
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseMovement then
        local Delta = Input.Position - DragStart
        Main.Position = UDim2.new(StartPosition.X.Scale, StartPosition.X.Offset + Delta.X, StartPosition.Y.Scale, StartPosition.Y.Offset + Delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Dragging = false
    end
end)

--========================================================--
-- REDIMENSIONAMENTO
--========================================================--

local ResizeHandle = Instance.new("TextButton")
ResizeHandle.Name = "ResizeHandle"
ResizeHandle.Size = UDim2.fromOffset(20, 20)
ResizeHandle.AnchorPoint = Vector2.new(1, 1)
ResizeHandle.Position = UDim2.new(1, -2, 1, -2)
ResizeHandle.BackgroundColor3 = COLORS.OliveDark
ResizeHandle.BackgroundTransparency = 0.05
ResizeHandle.BorderSizePixel = 0
ResizeHandle.Text = "◢"
ResizeHandle.TextColor3 = COLORS.Text
ResizeHandle.TextSize = 10
ResizeHandle.Font = Enum.Font.GothamBold
ResizeHandle.AutoButtonColor = false
ResizeHandle.ZIndex = 100
ResizeHandle.Parent = Main

local ResizeCorner = Instance.new("UICorner")
ResizeCorner.CornerRadius = UDim.new(0, 5)
ResizeCorner.Parent = ResizeHandle

local ResizeStroke = Instance.new("UIStroke")
ResizeStroke.Color = Accent
ResizeStroke.Thickness = 1
ResizeStroke.Parent = ResizeHandle

local Resizing = false
local ResizeStart
local StartWidth
local StartHeight

ResizeHandle.InputBegan:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Resizing = true
        ResizeStart = Input.Position
        StartWidth = Main.AbsoluteSize.X
        StartHeight = Main.AbsoluteSize.Y
        ResizeHandle.BackgroundColor3 = Accent
    end
end)

UserInputService.InputChanged:Connect(function(Input)
    if not Resizing then return end
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseMovement then
        local Delta = Input.Position - ResizeStart
        local NewWidth = math.clamp(StartWidth + Delta.X, MIN_WIDTH, MAX_WIDTH)
        local NewHeight = math.clamp(StartHeight + Delta.Y, MIN_HEIGHT, MAX_HEIGHT)
        Main.Size = UDim2.fromOffset(NewWidth, NewHeight)
    end
end)

UserInputService.InputEnded:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
        Resizing = false
        ResizeHandle.BackgroundColor3 = COLORS.OliveDark
    end
end)

--========================================================--
-- MINIMIZAR / BOTÃO FLUTUANTE
--========================================================--

local Minimized = false
local NormalSize = Main.Size
local MiniSize = UDim2.fromOffset(UI_WIDTH, 46)

local MiniButton = Instance.new("TextButton")
MiniButton.Name = "MiniButton"
MiniButton.Size = UDim2.fromOffset(58, 58)
MiniButton.AnchorPoint = Vector2.new(0.5, 0.5)
MiniButton.Position = UDim2.new(0.5, 0, 0.5, 0)
MiniButton.BackgroundColor3 = COLORS.Panel
MiniButton.BorderSizePixel = 0
MiniButton.Text = "NX"
MiniButton.TextColor3 = COLORS.Text
MiniButton.TextSize = 14
MiniButton.Font = Enum.Font.GothamBlack
MiniButton.AutoButtonColor = false
MiniButton.ClipsDescendants = true
MiniButton.Visible = false
MiniButton.ZIndex = 500
MiniButton.Parent = Gui

local MiniCorner = Instance.new("UICorner")
MiniCorner.CornerRadius = UDim.new(0, 12)
MiniCorner.Parent = MiniButton

local MiniStroke = Instance.new("UIStroke")
MiniStroke.Color = Accent
MiniStroke.Thickness = 2
MiniStroke.Parent = MiniButton

local MiniGradient = Instance.new("UIGradient")
MiniGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, COLORS.OliveDark),
    ColorSequenceKeypoint.new(0.5, COLORS.GreenDark),
    ColorSequenceKeypoint.new(1, COLORS.OliveDark)
})
MiniGradient.Rotation = 45
MiniGradient.Parent = MiniButton

local MiniScale = Instance.new("UIScale")
MiniScale.Scale = 0.7
MiniScale.Parent = MiniButton

local MiniParticleLayer = Instance.new("Frame")
MiniParticleLayer.Name = "Particles"
MiniParticleLayer.Size = UDim2.fromScale(1, 1)
MiniParticleLayer.BackgroundTransparency = 1
MiniParticleLayer.ClipsDescendants = false
MiniParticleLayer.ZIndex = 501
MiniParticleLayer.Parent = MiniButton

local MiniShineLayer = Instance.new("Frame")
MiniShineLayer.Name = "ShineLayer"
MiniShineLayer.Size = UDim2.new(1, -8, 1, -8)
MiniShineLayer.Position = UDim2.fromOffset(4, 4)
MiniShineLayer.BackgroundTransparency = 1
MiniShineLayer.BorderSizePixel = 0
MiniShineLayer.ClipsDescendants = true
MiniShineLayer.ZIndex = 501
MiniShineLayer.Parent = MiniButton

local MiniShine = Instance.new("Frame")
MiniShine.Size = UDim2.new(0, 4, 1, 0)
MiniShine.Position = UDim2.new(0, -10, 0, 0)
MiniShine.Rotation = 0
MiniShine.BackgroundColor3 = COLORS.Text
MiniShine.BackgroundTransparency = 0.7
MiniShine.BorderSizePixel = 0
MiniShine.ZIndex = 502
MiniShine.Parent = MiniShineLayer

local MiniShineGradient = Instance.new("UIGradient")
MiniShineGradient.Transparency = NumberSequence.new({
    NumberSequenceKeypoint.new(0, 1),
    NumberSequenceKeypoint.new(0.5, 0),
    NumberSequenceKeypoint.new(1, 1)
})
MiniShineGradient.Parent = MiniShine

task.spawn(function()
    while MiniButton and MiniButton.Parent do
        local shine = TweenService:Create(MiniShine, TweenInfo.new(1.6, Enum.EasingStyle.Linear), {Position = UDim2.new(1, 10, 0, 0)})
        shine:Play()
        shine.Completed:Wait()
        MiniShine.Position = UDim2.new(0, -10, 0, 0)
        task.wait(1.2)
    end
end)

task.spawn(function()
    local random = Random.new()
    while MiniButton and MiniButton.Parent do
        local particle = Instance.new("Frame")
        local size = random:NextInteger(2, 4)
        particle.Size = UDim2.fromOffset(size, size)
        particle.Position = UDim2.new(random:NextNumber(0.12, 0.88), 0, random:NextNumber(0.65, 0.9), 0)
        particle.BackgroundColor3 = random:NextNumber() > 0.5 and COLORS.Green or COLORS.Text
        particle.BackgroundTransparency = 0.15
        particle.BorderSizePixel = 0
        particle.ZIndex = 501
        particle.Parent = MiniParticleLayer
        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(1, 0)
        corner.Parent = particle
        local rise = TweenService:Create(particle, TweenInfo.new(random:NextNumber(0.8, 1.5), Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
            Position = UDim2.new(particle.Position.X.Scale + random:NextNumber(-0.15, 0.15), 0, random:NextNumber(0.02, 0.3), 0),
            BackgroundTransparency = 1
        })
        rise:Play()
        rise.Completed:Connect(function()
            if particle then particle:Destroy() end
        end)
        task.wait(0.22)
    end
end)

local function AnimarEntradaMini()
    MiniButton.Visible = true
    MiniScale.Scale = 0.25
    MiniButton.Rotation = -90
    TweenService:Create(MiniScale, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Scale = 1}):Play()
    TweenService:Create(MiniButton, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Rotation = 0}):Play()
end

local function AnimarSaidaMini()
    local escalaTween = TweenService:Create(MiniScale, TweenInfo.new(0.18, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Scale = 0.2})
    local rotacaoTween = TweenService:Create(MiniButton, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {Rotation = 90})
    escalaTween:Play(); rotacaoTween:Play()
    escalaTween.Completed:Wait()
    MiniButton.Visible = false
    MiniButton.Rotation = 0
end

local function AtualizarEstadoMinimizado()
    if Minimized then
        Main.Visible = false
        Tabs.Visible = false
        ResizeHandle.Visible = false
        for _, Page in pairs(Pages) do Page.Visible = false end
        AnimarEntradaMini()
    else
        AnimarSaidaMini()
        Main.Visible = true
        Main.Size = MiniSize
        TweenService:Create(Main, TweenInfo.new(0.38, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = NormalSize}):Play()
        Tabs.Visible = true
        ResizeHandle.Visible = true
        Pages.Main.Visible = true
        TabButtons.Main.BackgroundColor3 = Accent
        TabButtons.Main.TextColor3 = COLORS.Text
    end
end

Minimize.MouseButton1Click:Connect(function()
    if Minimized then return end
    Minimized = true
    Minimize.Text = "+"
    local shrink = TweenService:Create(Main, TweenInfo.new(0.22, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = MiniSize})
    shrink:Play()
    shrink.Completed:Wait()
    AtualizarEstadoMinimizado()
end)

MiniButton.MouseButton1Click:Connect(function()
    if not Minimized or MiniMoved then return end
    Minimized = false
    Minimize.Text = "—"
    AtualizarEstadoMinimizado()
end)

-- Arraste independente do botão flutuante (mouse e toque)
local MiniDragging = false
local MiniDragStart
local MiniStartPosition
local MiniMoved = false

MiniButton.InputBegan:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
        MiniDragging = true
        MiniMoved = false
        MiniDragStart = Input.Position
        MiniStartPosition = MiniButton.Position
    end
end)

UserInputService.InputChanged:Connect(function(Input)
    if not MiniDragging then return end
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseMovement then
        local Delta = Input.Position - MiniDragStart
        if Delta.Magnitude > 5 then MiniMoved = true end
        MiniButton.Position = UDim2.new(
            MiniStartPosition.X.Scale,
            MiniStartPosition.X.Offset + Delta.X,
            MiniStartPosition.Y.Scale,
            MiniStartPosition.Y.Offset + Delta.Y
        )
    end
end)

UserInputService.InputEnded:Connect(function(Input)
    if Input.UserInputType == Enum.UserInputType.Touch or Input.UserInputType == Enum.UserInputType.MouseButton1 then
        MiniDragging = false
    end
end)

--========================================================--
-- ABA INICIAL
--========================================================--

Pages.Main.Visible = true
TabButtons.Main.BackgroundColor3 = Accent
TabButtons.Main.TextColor3 = COLORS.Text

--========================================================--
-- NOTIFICAÇÃO INICIAL
--========================================================--

task.delay(0.5, function()
    Notify("NOXYS HUB ONLINE")
end)

print("[NOXYS HUB] Tactical Mobile Edition carregada.")